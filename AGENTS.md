<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version (16.2.12, Turbopack builds) has breaking changes â€” APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `frontend/node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## What this project is

SkyCare is a multi-tenant hospital management SaaS ("HMS SaaS"). Hospitals subscribe as **tenants** and get their own branded site at <slug>.skycare.app plus a staff dashboard (/app/*) and a patient portal (/patient, in progress). Every hospital manages its own admins/staff/patients; tenants are strictly isolated from each other.

**IMPORTANT: SkyCare is an INDEPENDENT project.** The Life Blossom project (C:\Users\Admin\Downloads\life-blossom-hosp) is a SEPARATE single-hospital app with its own git repo (liamsedun/lifeblossomhosp), its own Supabase project (hkqhsgdutaaufqqrekdx), its own env files and its own Netlify site. The two projects must NEVER be mixed: same-named files exist in both, but they are different codebases with different databases and deployments. All work in this workspace is SkyCare-only; never read from, write to, or reference the Life Blossom folder as part of SkyCare work.

## Repo layout

- `frontend/` â€” Next.js 16 app (App Router, Turbopack). All UI + API routes (`src/app/api/**`).
- `backend/supabase/` â€” SQL migrations applied to the linked Supabase project via `npx supabase db push` (run from `backend\supabase`). Migration `0001`â€“`0007` = pre-existing multi-tenant foundation; `0008` = HMS port (staff/expenses/other-income/doctor-notes/landing-doctors/internal mail/chats/bank accounts/duty roster/medical reports/push subscriptions/security events + audit triggers + RLS hardening); `0009` = staff backfill for seed users; `0010` = public `avatars` bucket + storage policies; `0011` = drop anon SELECT policy + revoke anon grants on `landing_doctors` (public reads served via the service client; APPLIED; see Gotchas below — CLI works via `npx supabase`). `0012` = `users.preferences` jsonb column.
- Prod Supabase ref: `pvakwxeusbxesdealuuc` (linked from `backend\supabase`, CLI logged in). Keys live in `frontend/.env.local` (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_ROOT_DOMAIN).

## Tenancy & isolation contract (CRITICAL)

- JWT claims live in `app_metadata { role, tenant_id, branch_id }`; RLS helpers in SQL read them: `get_tenant_id()`, `get_role()`, `get_branch_id()`, `is_super_admin()`, `is_hospital_admin()`, `is_staff()`, `is_patient()`, `is_family_primary()`, `family_patient_ids()`.
- Roles: `super_admin` (cross-tenant platform), `hospital_admin`, `doctor`, `nurse`, `pharmacist`, `lab_tech`, `cashier`, `receptionist`, `patient_api`. `ROLE_LABELS`/`getClaims`/`isStaffRole` in `frontend/src/lib/auth.ts`.
- **Every API route must be wrapped in `withAuth`/`withStaff`** (from `frontend/src/lib/api-utils.ts`), giving `ctx { user, claims, tenantId, branchId, role, supabase, svc }`. `requireTenant(ctx)` throws for non-tenant callers.
- **Service-client (`ctx.svc`) queries MUST filter `tenant_id = requireTenant(ctx)`** â€” service role bypasses RLS. `super_admin` with `tenantId === null` is the only platform-wide exception.
- `patient_api` is only accepted in patient-facing routes (appointments, dependants, invoices, payments declare, prescriptions, medical-records, lab-orders) and must resolve its family via `patients.user_id = ctx.user.id` (+ `primary_account_id` for dependants) before every query.
- RLS stays the safety net; the API layer is the authoritative guard. Never trust client-supplied tenant/org ids.

## Audit duality (who logs what)

- DB triggers `public.log_audit()` fire ONLY when `auth.uid()` is set (RLS-scoped writes). Service-role writes skip triggers (auth.uid() is NULL) and are logged by the API layer via `frontend/src/lib/audit.ts` (`logAudit`/`logView`/`flagSecurityEvent`/`checkLoginLockout`/`logAuth`).
- **Never double-log and never skip**: a write is logged by the trigger XOR the API, depending on which client wrote it. `logView` powers the rapid-view anomaly (>8 views/5 min â†’ `security_events`).

## API conventions

- Response envelopes: `ok(data, status?)`, `okPaginated(data, total, page, pageSize)` â†’ `{ success, data, meta: { page, pageSize, total, totalPages } }`. Errors: `ApiError` family (`AuthError` 401, `ForbiddenError` 403, `NotFoundError` 404, `ValidationError` 400) mapped by `withAuth`.
- `getPagination(searchParams)` â†’ `{ page, pageSize, from, to }` (range inclusive). `resolveParam` unwraps App Router params. `parseBody` for JSON.
- Role constants: `BILLING_ROLES = [hospital_admin, cashier, super_admin]`, `CLINICAL_ROLES = [hospital_admin, doctor, nurse, super_admin]`, `ADMIN_ROLES = [hospital_admin, super_admin]`.
- Number generators: `frontend/src/lib/tenant-settings.ts` (`generatePatientNumber`/`generateStaffNumber`/`generateInvoiceNumber`, prefixes from `tenants.settings` JSONB).
- Notifications: `frontend/src/lib/notify.ts` `notifyUsers(svc, { orgId, userIds, type, title, message, referenceType, referenceId })` â€” types: appointment_reminder, payment_declared/confirmed/cancelled, lab_result, prescription_refill, etc.

## Known gotchas

- `lab_orders` and `appointments` each have TWO FKs to `users` (doctor_id + created_by) â†’ embedding `users(...)` returns PostgREST PGRST201 (ambiguous). Always use FK hints: `users!lab_orders_doctor_id_fkey(...)`, `users!appointments_doctor_id_fkey(...)`.
- `chat_presence` PK is `user_id` (no `id` column) â€” `select=id` 400s; use `.select("*")` for wipes.
- **Node/undici fetch OVERRIDES the `host` header** with the URL host â€" host-based tenant resolution (`getHost()` in `[slug]/page.tsx` reads `x-forwarded-host` first) returns 404 in smoke tests unless you send `x-forwarded-host: <slug>.skycare.app`. PowerShell `Invoke-WebRequest -Headers @{host=...}` DOES pass host through (that's why manual checks pass while the script failed).
- **supabase CLI IS available via `npx supabase`** (run from `backend\supabase`; logged in, linked prod ref `pvakwxeusbxesdealuuc`) â€" use `npx supabase db push` to apply migrations. For ad-hoc SQL checks the CLI has no direct exec, but `npx supabase migration list` / `db push --dry-run` confirm remote state. Before this was discovered all migrations were applied manually in the Supabase SQL editor; CLI becomes authoritative going forward.
- `expenses`/`other_income`.`payment_method` is free `text` (default 'cash'), NOT the `payment_method` enum.
- `medical_records.record_type` is a text CHECK (9 values), not an enum; confidential records are RLS-hidden from `patient_api`.
- Family model: `patients.is_primary_account`, `primary_account_id`, `dependant_relationship`; cap 5 dependants; dependants link to the primary holder only.
- Seed/demo: **since Aug 12 round 3, the ONLY tenant in prod is the user's fresh registration** — "LiamsField Hospital" (`b9290c07-456f-4951-ae86-37a64dacc486`, slug `liamsfields`, admin `olalekan.edun@gmail.com`, website_url NULL) (see purge + onboarding entries below).
- Dashboard server pages fetch directly via RLS client (`createClient()`, `frontend/src/lib/supabase/server.ts`); heavy/cross-table reads go through API routes with the service client.
- `frontend/src/lib/nav.ts` had `soon: true` nav items; all are now landed (Settings was the last) â€” no `soon` flags remain.
- **supabase-js auth-session takeover (smoke-killer)**: calling `SVC.auth.signInWithPassword` (etc.) on a SERVICE-ROLE client REPLACES the REST client's `Authorization` header with the signed-in user's token — every subsequent `SVC.from(...)` call silently runs under RLS as that user; service-role deletes then "succeed" matching 0 rows (200 + `[]`, no error). This leaked QA fixtures into prod twice (branch-price smoke cleanups). **Never sign in on the same client used for service-role writes** — use a separate `createClient` for auth/session-cookie work.
- **ESM scripts under `temp\opencode\` must pin the supabase-js copy**: bare `import "@supabase/supabase-js"` resolves via the file's dir walk-up → `C:\Users\Admin\node_modules` (stale v2.112.1) instead of the project's (frontend v2.111.0); same class of delete weirdness. Use `createRequire(\`${process.cwd()}/package.json\`)` (run from `frontend\`). Verify delete didn't silently 0-match: deletes with `.select("*")` MUST return the deleted row(s); check `gone()` after.
- **Never run convert-sale/dispense legs inside pharmacy smokes** — they write append-only `dispensing_audit_logs` (immutable trigger `trg_dal_immutable` raises for every role incl. service); a polluted run REQUIRES a migration (`0071`–`0073`, `0080` pattern: drop trigger, purge, recreate). Also delete stock batches BEFORE the parent drug in cleanup, then sweep `pharmacy_branch_stock`, or the batch-delete AFTER trigger re-inserts it mid-cascade (23503 — blocks the drug delete via API).

## Work State (Aug 2026)

### Completed
- **Family page: PRIMARY patient card now pulls the portal profile photo (avatar fallback, Aug 17)** — user report: PT-0001 Adeolu Adesanya's card on `/patient/family` showed initials "AA" instead of his profile photo. **Root cause**: the portal PROFILE photo upload (`POST /api/uploads/avatar`) stores to `avatars/users/<uid>/…` and sets **`users.avatar_url` only** — never `patients.avatar_url` (migration 0087, which the family pages read) — so any patient who uploaded via Profile showed initials on family cards (confirmed live: PT-0001 + PT-0012 Ada Okafor both have `users.avatar_url` set, `patients.avatar_url` NULL). **Fix (read-side fallback, no migration)**: `patients` has exactly ONE FK to `users` (`user_id`, migration 0005) so a plain `users(avatar_url)` embed is unambiguous — `GET /api/patients/me` family select and `GET /api/dependants` select both gained `users(avatar_url)`, each row mapped `avatar_url: patients.avatar_url ?? users.avatar_url ?? null` (family-page uploads keep precedence over profile photos; the embed is stripped from payloads). Covers every consumer: family list card, detail ProfileCard/Edit-modal preview, appointments Book picker, dashboard chips. TS gotcha: supabase-js generic typing rejected `rest.avatar_url` on the embed row — cast `row as Record<string, unknown> & { users?: { avatar_url?: string | null } | null }` before destructuring.
  - **Verified**: build green (Next 16.3.0 Turbopack); prod restarted on :3000; new `temp\opencode\family-avatar-fallback-smoke.mjs` **14/14 vs prod** (primary + dependant with users.avatar_url set but NULL patients.avatar_url: me payload falls back for both, no `users` embed leaked, setting patients.avatar_url flips precedence, patient-scoped + staff-scoped `GET /api/dependants` fall back, dependant session sees it, both family pages 200; zero leftovers); regressions `family-page-smoke.mjs` **30/30** + `patient-mobile-nav-smoke.mjs` **29/29**.
- **Portal greetings: 👋 replaced with Sun/MoonStar icons (Aug 17)** — user asked to remove the waving hand from both portals' greetings and show a sun/moon that matches the time of day:
  - **Patient portal** (`patient-dashboard.tsx`, both web hero + mobile header): `isNight = new Date().getHours() >= 17 || new Date().getHours() < 5`; when night renders `MoonStar` else `Sun` (web: `text-white/90` 26px inside the hero; mobile: `text-amber-500` 20px). Emoji removed from both trees.
  - **Staff portal** (`dashboard-view.tsx` hero): same `isNight` logic; h1 gained `mt-2 flex items-center gap-2 text-2xl font-bold sm:text-3xl` with the `MoonStar`/`Sun` icon at `text-white/90` 26px beside the greeting text.
  - **Verified**: clean build green; prod restarted on :3000; `patient-mobile-nav-smoke.mjs` **29/29** + `mobile-nav-smoke.mjs` **46/46**; deployed SSR chunk for staff dashboard verified via the h1 className + the `m.MoonStar`/`q.Sun` ternary (see chunk-verification gotcha below).
  - **Chunk-verification gotcha (learned this session)**: Turbopack minifies lucide imports to short aliases — `MoonStar`/`Sun`/`isNight` may NOT appear as literals in built chunks. Verify via stable strings instead: the h1 className (`mt-2 flex items-center gap-2 text-2xl font-bold sm:text-3xl`) or the ternary shape (`(0,b.jsx)(m.MoonStar,...):(0,b.jsx)(q.Sun,...)`). Also: a "stale" chunk that has NEITHER old NOR new content is a broken/mixed build artifact — `Remove-Item .next -Recurse -Force` then rebuild; the clean rebuild produced correct output.
- **Patient portal Family: Life-Blossom redesign + new per-member detail page (Aug 17)** — `/patient/family` (`patient-family.tsx`) fully rewritten to match the LB dependants list (READ-ONLY reference) and a new `/patient/family/[id]` detail page (`patient-family-detail.tsx` + route) was added, with LB-shaped avatar upload + portal-login support in the Add/Edit modals:
  - **API**: migration `0087_patients_avatar_url.sql` (APPLIED) adds `patients.avatar_url`. `GET /api/patients/me` family select extended `allergies, chronic_conditions, avatar_url, created_at`; `GET /api/dependants` select adds `avatar_url`. `POST /api/dependants` accepts `avatar` data URL — uploads after insert, re-selects + returns the FRESH row (a first version returned the pre-avatar row — the DB had the URL but the response was stale; smoke asserted the response and failed), and on upload failure deletes the dependant + any portal account + 400. `PUT /api/dependants/[id]` accepts `avatar` (string → store+set; null/"" → clear `avatar_url`). Existing behavior preserved: POST auto-provisions a portal login from plain `email` when no `portalEmail` is given (fallbackEmail).
  - **Name-collision lesson**: `frontend/src/lib/patient-avatar.ts` ALREADY existed (patientGradient/patientInitials/PATIENT_GRADIENTS used by the staff Patients page) — the new avatar helpers (`parseAvatarDataUrl`/`storePatientAvatar`, path `patients/<tenantId>/<patientId>-<ts>.<ext>`) were APPENDED to it, restoring the original exports.
  - **New libs**: `square-image.ts` (`fileToSquareImage` — canvas square-crop → 512px jpeg data URL, 2MB cap), `patient-family-shared.ts` (FamilyMember type, MAX_DEPENDANTS 5, LB relationship vocab Child/Spouse/Parent/Sibling/Grandparent/Other stored lowercase, BLOOD_GROUPS/GENOTYPES, REL_INFO/relLabel, ageOf/fmtDate/ngn/statusOf (needs_attention when allergies or outstanding>0)/outstandingOf).
  - **List page (LB design)**: AppHeader "Dependants" + back-to-overview link, "Family Account" card (gold/wine gradient, primary-holder tiles w/ gold "Main" badge, family code = primary's patient_number, live "N of 5 slots used" progress + gold CTA "Add member" — dashed when max reached, with banner + slide-up sheet telling the primary to contact the hospital), member cards (navy avatar tile w/ gradient initials or photo, Active/Needs-attention chip, gold code·number, age·gender, blood/genotype icons, relationship w/ relInfo hint, outstanding ₦) — every card links to the detail page, dashed "Add Dependant" card, gold `UserPlus` FAB → Add modal (photo upload w/ preview, shared fields, portal-login checkbox, "email auto-provisions a login" note removed — portalEmail only presents password when checked), note "dependants can manage their own profile"; remove lives on the detail page ONLY (list has no remove — leftover AppSheet/removeTarget/GhostButton imports were purged after the redesign).
  - **Detail page `/patient/family/[id]`**: back link "Back to overview", primary/dependant sibling chips, navy profile card (avatar/initials + camera edit, allergy warning banner "Needs attention"), 3 stat tiles (Age · Blood group, Outstanding invoices, Upcoming appointments), 4 LB-style tabs — Biodata (personal facts grid + Edit), Medical Records, Bills, Appointments (all fetched family-scoped via `?patient_id=`); Edit modal (PUT snake_case + avatar swap/clear "Remove photo"), Remove (SOFT delete — confirm says "removed from your family account; their records remain available to your hospital", NOT "permanently deleted"), Pay modal (online via `/api/payments/initialize` when gateway enabled, else bank_transfer w/ bank details / POS via declare), Book appointment link → `/patient/appointments`. Back-link note: family_code for dependants = the ROOT primary's patient_number (root = `item.primary_account_id`, not their own).
  - **Verified**: build green (Turbopack, 2 rebuilds); prod restarted on :3000; `C:\Users\Admin\AppData\Local\Temp\opencode\family-page-smoke.mjs` **30/30 vs prod** (me family fields, POST avatar → response has URL + me-fetch persists; email auto-provisions portal login (user_id + tempPassword present); detail page 200; bad avatar 400 + nothing created; PUT avatar swaps URL; PUT null clears DB + file still public when re-checked at the PREVIOUS URL; family APIs readable: own + sibling invoices, medical-records, appointments; dependants GET has avatar_url; cleanup zero leaks — avatar object, patients (child/spouse `rows=0` is the primary_account_id CASCADE from the primary delete — expected), users mirrors, auth, audit `%QA-FF%`); regressions: `patient-mobile-nav-smoke.mjs` **29/29** + `mobile-nav-smoke.mjs` **46/46**. Bundle: "Dependants"/"Family Account"/"Add Dependant"/"Needs attention"/"Primary holder"/"slots used"/"Total outstanding" present in deployed chunks.
  - **Smoke gotchas learned (this run)**: GoTrue REJECTS the `@qa.local` TLD for admin.createUser (use `@qa.com`); `users` has NO `password_plain` column; `SVC.auth.signUp` is rate-limited — use `SVC.auth.admin.createUser` (email_confirm: true); `fetch(null)` throws "Failed to parse URL from null" — the avatar-public check must use the LAST known good URL (captured before the PUT-null clears it), not a fresh me-fetch (fails because the URL is already null by then).
- **Mobile footer: BLUE footer card in light mode + icon tints that pop on it (Aug 17)** — user follow-up: in LIGHT mode the bottom-nav footer dock in the Admin & Staff portal AND the Patients & Dependants portal should get a BLUE background, with the colorized icons still displaying well on it:
  - **CSS-first approach** (no JS theme reads → zero hydration mismatch; the app theme lives on `html[data-theme]`, NOT the OS-driven `dark:` variant): new unlayered rules in `globals.css` scoped `html:not([data-theme="dark"])` — (1) `.mobile-nav-bar` becomes a **blue gradient** `linear-gradient(180deg,#3b82f6 0%,#2563eb 58%,#1d4ed8 100%)` with translucent-white border + blue-tinted dock shadow (replaces the token frosted bar); dark mode untouched (token bar via the component's own `bg-[var(--color-background)]/85` utility — unlayered CSS only fires in light). (2) **Per-key icon tints switch to bright 300-level pastels** that read on blue: overview `#dbeafe`, patients `#86efac`, appointments `#c4b5fd`, pharmacy `#fcd34d`, lab/lab-results/family `#67e8f9`, billing `#5eead4`, chats/profile `#f9a8d4`, mail/internal-mail `#fdba74`, prescriptions `#fda4af`, records `#a5b4fc`, notifications `#e2e8f0`, account `#fde047`, download `#d8b4fe` — applied as `html:not([data-theme="dark"]) .mobile-nav-icon[data-key="…"] { color: … !important }` (needs `!important`: the component still sets the saturated dark palette inline via `FOOTER_COLORS`, and inline style beats any non-important stylesheet rule). (3) Light-mode active tab capsule/dot/glow switch blue→white: `.mobile-tab-capsule` `rgba(255,255,255,0.18)` bg + white `--tw-ring-color`, `.mobile-tab-shine` white via-color, `.mobile-tab-glow` white blur halo, `.mobile-tab-dot` solid white + white glow; icons get a navy-edge + white-glow drop-shadow in light mode for extra pop.
  - **Component**: `mobile-nav.tsx` nav element gains `mobile-nav-bar`; each tab `<Icon>` gains `mobile-nav-icon` + `data-key={item.key}` (needed for the per-key overrides); the active capsule/shine/glow/dot spans gain `mobile-tab-capsule`/`mobile-tab-shine`/`mobile-tab-glow`/`mobile-tab-dot` hooks. Both portals share `MobileNavBar` → one change covers staff + patient footers.
  - **Cascade notes**: unlayered CSS beats Tailwind v4's `@layer utilities` classes regardless of order → the light overrides win over `bg-[var(--color-background)]/85`, `border-[var(--color-border)]`, `shadow-[…]`, `bg-[var(--color-primary)]/15`, `ring-…` etc.; Turbopack minifier collapses `linear-gradient(180deg,…)` → `linear-gradient(…)` and `#ffffff47`-style 8-digit hexes, so chunk greps for the gradient must accept the collapsed form (`#3b82f6 0%,#2563eb 58%,#1d4ed8 100%` found in the CSS-bearing chunk; the `!important` hex rules verified via `#dbeafe`/`86efac`).
  - **Verified**: clean build green (Next 16.3.0 Turbopack); prod restarted on :3000; CSS chunk contains `.mobile-nav-bar{background:linear-gradient(#3b82f6 0%,…#1d4ed8 100%)…` + `.mobile-nav-icon` dropshadow + data-key rules; SSR chunks `src_01r541x._.js` + `src_0hr762h._.js` carry `mobile-nav-bar` + `data-key=` markup; regressions `mobile-nav-smoke.mjs` **46/46** + `patient-mobile-nav-smoke.mjs` **29/29**. Dark mode behavior byte-identical (all overrides scoped to light).
- **Mobile footer icons COLORIZED + thinned in BOTH portals (Aug 16)** — user feedback: the footer tab icons in the Admin & Staff portal AND the Patients & Dependants portal were "too bold". `mobile-nav.tsx` (shared `MobileNavBar` renders both footers) now:
  - **Per-key icon colors** via a new `FOOTER_COLORS` map (saturated mid-400 hues, distinct from the pastel `TILE_COLORS` sheet-tile backgrounds): overview `#60a5fa` blue, patients `#34d399` emerald, appointments `#a78bfa` violet, pharmacy `#fbbf24` amber, lab / lab-results / records-family-cyan `#22d3ee` / `#818cf8` indigo records, billing `#2dd4bf` teal, chats / profile `#f472b6` pink, mail / internal-mail `#fb923c` orange, prescriptions `#fb7185` rose, notifications `#94a3b8` slate, account `#facc15` yellow, download `#c084fc` purple — applied as `style={{ color: FOOTER_COLORS[item.key] ?? "var(--color-primary)" }}` directly on the lucide `<Icon>`.
  - **Thinner strokes** — `strokeWidth={active ? 1.6 : 1.4}` (was 2.4/2.0) so the icons read light, never bold. Inactive icons get `opacity-80` (→ 100 on hover) so active-tab state still reads via the primary-tinted capsule + full-color icon while idle tabs stay colorized-but-muted. Sheet tiles/FAB/theme tokens untouched.
  - **Verified**: clean build green (Next 16.3.0 Turbopack); prod restarted on :3000; `mobile-nav-smoke.mjs` **46/46** + `patient-mobile-nav-smoke.mjs` **29/29 vs prod** — the patient smoke's failing check was a STALE ASSERTION, not a regression: `PATIENT_TABS` in the smoke still listed "Lab Results" as a footer tab, but the earlier Lab-into-More-menu batch removed it (evidence: 2012-entry chunk check). Smoke updated: tabs array now 5 keys (`Overview, Appointments, Bills &amp; Payments, Chats, Messages`) and the More-sheet tile loop asserts `>Lab Results<` (Lab Results now lives in the menu). Deployed SSR chunks `src_01r541x._.js` + `src_0hr762h._.js` (20232B, found via `-match "Open More menu"`, NOT by mobile-nav filename — chunk hashes renamed after rebuild) both contain all 8 checked FOOTER_COLORS hexes + `strokeWidth` + `opacity-80`. **Root cause**: `/api/patients/me` returns `ok({selfId, rootId, family})` (success envelope), but the dashboard read `meBody.family` — always undefined → `setFamily([])`. The SAME bug existed in `patient-family.tsx` (`body.family`/`body.rootId`/`body.selfId` — the whole dependants page showed "No family account yet" despite registered dependants) and `patient-appointments.tsx` (`body.family` — Book modal patient picker was empty). All three now read `body.data?.…`. Bonus: the dashboard identity card (Patient No/phone/DOB) was ALSO blank because `patientMe` came from the same broken family list. **Family card made Life-Blossom-like**: new `dependants` memo (`family.filter(m => !m.is_primary_account)` — primary holder excluded, matching LB's dependent-count semantics); Family stat card shows N dependants; chips card lists dependants only (gold initial circle + first name, inactive warning icon); card header "Family" is now a clickable Link (with gold chevron) + "Manage" gold link — both open `/patient/family`, the dependants page where the main (primary) patient can ADD members. Verified: clean build green (Turbopack); prod restarted on :3000; deployed SSR chunks checked — dashboard `data?.family` + dependants filter present, family page `data?.family`/`data?.rootId`, appointments `data?.family`.
- **Patient portal: identity card phone/DOB + quick-action Prescription button (Aug 16)** — follow-up on the mobile app conversion. (1) The **profile identity card** (mobile view, `patient-profile.tsx`) only showed avatar/name/patient no/email — the user asked for the PATIENT's PHONE NUMBER AND DATE OF BIRTH pulled from the patient's profile: `PatientRow` gained `phone` (the `/api/patients/me` payload already carries it) and the card now renders a Phone + Date of Birth rows block (uppercase muted labels / right-aligned semibold values on `bg-black/[0.03]`) under the avatar header. (2) The **Quick Actions card** (dashboard, `patient-dashboard.tsx`) gained a **Prescription** pill (`from-cyan-500 to-sky-400` gradient) linking `/patient/prescriptions`. Dashboard identity card already had phone/DOB — untouched. Verified: clean build green (Turbopack), prod restarted on :3000, deployed SSR chunks `src_components_patient_patient-dashboard_tsx_1tx0_ty._.js` + `src_components_patient_patient-profile_tsx_06qnmyc._.js` contain "Prescription"/`/patient/prescriptions`/cyan gradient + "Phone"/"Date of Birth"/`?.phone` respectively.
- **Patient footer: Lab icon moved from bottom tabs into the More menu (Aug 16)** — the user asked to move the LAB icon out of the patient footer tabs and into the `PATIENT_MENU`, placed BEFORE Prescriptions. `mobile-nav.tsx` `PATIENT_TAB_KEYS` dropped `"lab-results"` (footer is now Overview/Appointments/Bills & Payments/Chats/Messages — 5 tabs) and `PATIENT_MENU` starts with `{ key: "lab-results" }, { key: "prescriptions" }, …` — the tile resolves from `PATIENT_NAV_ITEMS` (label "Lab Results", FlaskConical icon). Build green (Turbopack), prod restarted on :3000, deployed SSR chunk `src_0hr762h._.js` verified: new 5-key tabs array present + the `{key:"lab-results"},{key:"prescriptions"}` menu head; old 6-key tabs array absent.
- **Patient portal mobile app conversion — ALL pages except chats (Aug 16, ongoing user session)** — SkyCare's `/patient` app now matches the user's Life Blossom patient app on small screens (`<md` 768px) while `md+` keeps the web versions; exemptions: light/dark themes, patient bottom-nav + nine-dots More FAB, gold `#e0a84a` in both themes:
  - **Kit** `frontend/src/components/patient/mobile/mobile-app-ui.tsx`: AppHeader (title + meta + gold underline via `after:`), AppSegmented (title-case options, gold active bar), AppSheet (slide-up, backdrop, gold grab handle + close), AppFab (gold gradient), AppSkeletonList (3 pulsing glass rows), AppStatusChip (gold/rose/emerald/slate translucent variants), GhostButton, GlassCard, `cn` helper — **IMPORTANT: `@/lib/utils` does NOT exist in this codebase — `cn` is defined + exported here and imported from mobile-app-ui, never from `@/lib/utils`**.
  - **Theming gotcha (recurring)**: Tailwind v4 CSS-first — the default `dark:` variant follows OS pref, NOT the app theme (`html[data-theme]` remaps in globals.css) → mobile trees must use remapped utilities/tokens (`text-amber-600`, `bg-sky-100`, `border-[var(--color-border)]`, `text-[var(--color-muted-fg)]`); one `dark:bg-white/[0.02]` slip in prescriptions was replaced with tokenized border. Life Blossom identity card stays DARK NAVY in both themes; page bg/cards adapt via tokens.
  - **Dual-render pattern (all pages)**: web tree in `hidden md:block`, mobile tree in `md:hidden`, shared state/fetches/errors; **modals/sheets/errors hoisted OUTSIDE both wrappers** (fixed overlays inside a `display:none` ancestor don't render): BookModal, DeclareModal, AddMemberModal, NoteEditModal, password modal, cancel/remove AppSheets.
  - **Pages converted** (`src/components/patient/*.tsx`, each with its own AppHeader meta + gold icons/CTAs + AppStatusChip + expandable cards): appointments (AppSegmented Upcoming|Past via `mobileTab`, Confirm/Cancel, AppSheet cancel confirm, gold AppFab → BookModal; NO reschedule — SkyCare has no reschedule API), billing (balance card w/ Wallet tile, Outstanding|Paid segmented, expandable bill cards w/ items/VAT/payments/View Receipt link, gold "Declare Payment"), prescriptions (gold Pill tile, medication lines + notes on expand), lab-results (gold FlaskConical tile, per-test Normal/Abnormal chips + "Reported …"), records (3-tab segmented reusing existing `tab`, **gold timeline** `border-l-2 border-[#e0a84a]/30` + gold dots `shadow-[0_0_0_3px_rgba(224,168,74,0.25)]`, PenLine edit → shared NoteEditModal), family (relationship-gradient initials tiles, Main account gold badge, tel:/mailto: rows, Remove flow via AppSheet, gold AppFab → AddMemberModal), notifications (All/Unread segmented, gold "Mark All Read", unread = gold-tinted cards + gold dot), mail (Inbox (n)/Sent/Compose segmented; compose form hoisted to a `composeForm` const shared by both trees), profile (identity card 72px avatar w/ gold ring + camera upload, info rows via extracted `infoList`, Change Password/Sign Out; one shared modal at root).
  - **patient-chats.tsx deliberately NOT converted** — already an immersive navy/teal two-pane app style (list/thread, back chevron `md:hidden`, list `md:flex`) matching Life Blossom; leave untouched.
  - **Verified**: clean build green (after fixes: dropped `const visible` restored, `GOLD_GRADIENT`/`AppFilterChips` unused imports pruned); prod restarted on :3000; smokes: `temp\opencode\patient-mobile-nav-smoke.mjs` **30/30** + `mobile-nav-smoke.mjs` (staff) **46/46** vs prod; deployed-chunk recursive grep (`.next/server/chunks` incl. `ssr/` — non-recursive searches miss the ssr copies; readFileSync must use the ABSOLUTE path, joining again path-joins the joined path): mobile markers present for ALL pages — appointments/billing/prescriptions/lab-results/records/family/notifications/mail/profile chunks + `app-glass`/`app-mobile-bg`/gold-timeline CSS in patient chunks; dashboard spot-check via "Care snapshot"/"No upcoming appointments." in `src_components_patient_patient-dashboard_tsx_1tx0_ty._.js`.
- **Financial Report: "Income by Module" → "Income by Services" + mobile card layout (Aug 16)** — user follow-up: the module-income card was illegible on small screens ("only about half of the card is showing"):
  - **Rename everywhere user-facing** (`financial-reports-view.tsx`): section heading → "Income by Services", table header `Module` → `Service`, total row "All Modules" → "All Services", pie subtitle "Collected share by module" → "...by service", Collected-vs-Invoiced subtitle "...by module" → "...by service", CSV export header row + "All modules" → "All services", print-doc letterhead "A. INCOME BY MODULE" → "A. INCOME BY SERVICES". Code identifiers (`ModuleIncome`, `moduleRows`, `MODULE_META`) unchanged.
  - **Mobile responsiveness**: the card's table is `min-w-[520px]` inside `overflow-x-auto` — on phones only the first columns were visible. The table is now `hidden md:block` and a new **`md:hidden` stacked card list** renders before it: per service = one rounded card (icon chip + label + count pill; then a 3-col grid Invoiced / Collected (emerald) / Outstanding with `truncate`, `active:scale-[0.98]` press feedback) + a totals bar card ("All Services", invoiced + collected emerald / outstanding + truncated naira values).
  - **Verified**: clean build green (Next 16 Turbopack); prod restarted on :3000; deployed SSR chunk `_1z7mamy._.js` contains "Income by Services" ×1, "All Services" ×2, "INCOME BY SERVICES" ×2, `md:hidden` ×1 and `min-w-[520px]` ×1 (table still present for md+). No API/DB changes; mobile-nav smokes unaffected (component untouched).
- **Mobile bottom-nav + floating More FAB for staff/admin (Aug 16)** — the user asked for a mobile app footer (bottom navigation) with a right-side floating "More" pop-up grid, per a detailed spec; a vanilla single-file prototype was delivered first (`temp\opencode\skycare-mobile-nav.html`), then ported into the Next.js app:
  - **`frontend/src/components/dashboard/mobile-nav.tsx`** (NEW, client component, rendered by `app-shell.tsx` on every `/app` page; wrapper `fixed inset-x-0 bottom-0 z-50 md:hidden` so it only shows below the md breakpoint where the sidebar is hidden): six flat tabs — Overview, Patients, Appointments, Pharmacy, Lab, Billing (icon + label + active dot, active amber `#f0a93a`, inactive `#59636f`, on a near-black bar `#0c0f14` with `#1e2631` top hairline + `env(safe-area-inset-bottom)` padding) — plus a **floating "More" FAB at the far right** (56px amber gradient circle raised `-translate-y-[18px]` above the bar, `Grid3x3` ↔ `X` icon morph on open). The FAB opens a dark rounded sheet (`#151a24`/97, rounded-2xl, right-3 bottom-[78px], `max-w-[350px]`, triangular tail at right-[38px] pointing at the FAB, dimming `bg-black/55 backdrop-blur-[2px]` overlay, scale-[0.92]→1 + opacity transition, Esc/overlay/FAB-again/route-change close) with a **3-column grid of 17 tiles** (44px rounded-xl pastel badge + dark lucide glyph + 10px label): Ward, Other Income, Staff, Human Resources, Payroll, Leave, Roster, Mail, Chat, Medical Reports, Financial Reports, Audit Logs, Accounts, Subscription, Download App, Profile, Settings. Group tiles (Ward/HR/Payroll) carry an amber N-count chip and expand a chip row (grid-rows 0fr→1fr animation) of their sub-pages; tiles with children are real `Link`s (navigation + auto-close), Mail tile shows `UnreadMailBadge`. **Everything is built from `navForRole(role, moduleAccess)`** — role gates and per-user module_access grants apply exactly like the sidebar; tiles for non-granted modules are skipped. Label overrides vs nav: wards→"Ward", hr→"Human Resources", roster (alias of the hr-roster child, CalendarRange icon)→"Roster", chats→"Chat", account→"Accounts". `app-shell.tsx` main gained `pb-28 md:pb-6` so content clears the fixed bar on mobile.
  - **Verified**: build green; prod restarted on :3000; `temp\opencode\mobile-nav-smoke.mjs` **31/31 vs prod** (SSR HTML of `/app` with a QA hospital_admin session: bar + FAB + md:hidden + all 6 tabs + all 17 tiles with the spec labels + Ward count chip 5/HR 5/Payroll 4 + `pb-28` present; zero leftovers — users mirror + auth swept). **Smoke lessons**: tiles whose labels differ from nav need the explicit override applied in the map (the first run passed "Wards"/"HR"/"Shifts & Roster" — caught by asserting `>Ward<` etc.); submenu chips are hydration-only (closed state SSR omits them — don't assert chips in SSR); nav "Chats" needs the spec label "Chat".
  - **Follow-up (same day) — theme-token footer + icon-only tabs**: the user reported the footer icons were "set to dark mode default and not visible" (the bar was hard-coded `#0c0f14`/`#59636f`/amber regardless of theme). Recolored the whole component to the app's theme tokens so it flips with the built DARK/LIGHT system: bar `bg-[var(--color-background)]` + `border-[var(--color-border)]`, inactive tabs `text-[var(--color-muted-fg)]`, active tab + dot `text-[var(--color-primary)]` (dark = gold, light = sky), sheet `bg-[var(--color-background)]` + `border-[var(--color-border)]` + tail matching, sheet header/tile labels `var(--color-foreground)`/`muted-fg`, group chips `var(--color-muted)`/`foreground`/`border` with active chip `primary-soft`/`primary-dark`. **Bar tab labels removed — icons only** (aria-labels kept for a11y; active tab shows the primary dot; the More-sheet tiles keep their labels). FAB stays amber-gradient in both themes (spec accent). Smoke extended to **40/40** — token classes present in SSR, tab anchors contain no `>Label<` text (the naive global `!html.includes(">Overview<")` check FAILS because the desktop sidebar SSR still renders those labels — scope assertions to the mobile-nav `<a>` element), aria-labels kept, Overview active on /app, all 17 tiles + chips still present.
  - **Follow-up 2 (same day) — nine-dots FAB + sheet overflow fix**: (1) the More FAB icon is now a **3×3 nine-dots pattern** (custom inline SVG — nine `r="1.8"` circles at 4.5/12/19.5 positions, `fill="currentColor"`; `Grid3x3` import dropped) instead of squared cells, still morphing to the ✕ on open; (2) **Profile & Settings cut off on short screens** — root cause: the scroll region had a fixed `max-h-[calc(100vh-220px)]` which clipped the 6th tile row on small viewports (SSR/tile presence was never the problem). Sheet is now `flex flex-col max-h-[min(560px,calc(100vh-90px))]` with a `shrink-0` header and a `min-h-0 flex-1 overflow-y-auto` scroll region — the sheet can never exceed the viewport and the grid always scrolls, so the last row (Profile, Settings) is always reachable. Build green, prod restarted on :3000, smoke **44/44** (nine-dot circles present, `Grid3x3` absent, flex-col + capped max-height + flex-1 scroll classes present, all prior 40 checks green).
- **Follow-up 3 (same day) — ported to the PATIENT portal / ALL screens**: refactored `mobile-nav.tsx` into a generic bar — `MobileNavBar({ spec: { root, tabs, menu, mailHref } })` + shared tile-color map (`MobileNavSpec`/`MobileNavTile`); the **default export `MobileNav` (staff)** builds its spec from `navForRole` exactly as before (44/44 smoke pristine), and a new named export **`PatientMobileNav`** builds the patient spec from `PATIENT_NAV_ITEMS`: bar tabs = Overview, Appointments, Bills & Payments, Lab Results, Chats, Messages (icon-only, aria-labels); More sheet = Prescriptions, Medical Records, Family, Notifications, Profile + **Account/Download App** (their keys are NOT in `PATIENT_NAV_ITEMS` — the menu builder must fall back to the spec entry's own href/icon when `index.get(key)` is undefined, or the tile silently vanishes); no group children → no chips (the ONLY `aria-expanded` in the patient bar is the FAB). `patient-shell.tsx` renders `<PatientMobileNav />` and its main gained `pb-28 md:pb-6`. Same nine-dots FAB/sheet/theme tokens; `UnreadMailBadge` follows `spec.mailHref` (`/app/mail` staff vs `/patient/internal-mail`). Build green, prod restarted on :3000; staff smoke **44/44**, new `temp\opencode\patient-mobile-nav-smoke.mjs` **30/30 vs prod** (QA patient_api: bar + FAB + md:hidden + 6 icon-only tabs + 7 tiles + no chips + `pb-28`; zero leftovers — users mirror + auth swept, audit 0). **Port smoke gotchas**: (1) HTML-escaped attributes — "Bills & Payments" SSRs as `aria-label="Bills &amp; Payments"`, probes must search the escaped form; (2) naive global `aria-expanded` count FAILS (the patient header bell/notifications dropdown also carries one) — scope the count to the nav section via `html.indexOf('class="fixed inset-x-0 bottom-0 z-50 md:hidden"')`.
- **HR Shifts & Roster: shift TEMPLATES editable + deletable (Aug 16)** — the user asked to edit shift templates already in the system (previously create-only, `New shift template` button + `POST /api/hr/shifts` only):
  - **API** `PUT|DELETE /api/hr/shifts/[id]` (NEW route; `withStaff` + `isHrAdmin` — doctor 403): PUT patches name/start_time/end_time (+ base-fallbacks to existing values when the body omits them), department/color/is_active presence-based; duplicate-name check case-insensitive EXCLUDING self (`ilike` + `neq`) → 400 "A shift template with this name already exists"; unknown id 404; TIME_RE validation; audit "Updated shift template"; returns the updated row. DELETE tenant-scoped, 404 unknown, audit ("Deleted shift template"); `staff_shifts.shift_id` is ON DELETE SET NULL so assignments keep their rows but drop the shift reference (confirm dialog says so).
  - **UI** (`hr-roster-view.tsx`, admins only): "New shift template" now routes through `openNewShift()` (fresh form); a second **"Shift templates"** `List`-icon button opens a manage modal — every template renders a color dot, name, Active/Inactive chip, `HH:MM – HH:MM · dept · ward` line, per-row **Edit** (`openEditShift` pre-fills the form → same modal titled "Edit shift template", submit → PUT → "Save changes") and **Delete** (confirm + `DELETE`); modal re-opens the form and reloads on save. Empty state hint present.
  - **Verified**: build green (Next 16 Turbopack); prod restarted on :3000; `temp\opencode\hr-shift-edit-smoke.mjs` **11/11 vs prod** (create 2 → PUT renames/times/color persisted → dup-name 400 (case-insensitive — smoke lesson: after editing c1, its ORIGINAL name is free, so the dup test must target the CURRENT name of the other template) → bad time 400 → unknown 404 → doctor PUT/DELETE 403 → GET list shows edited + untouched → DELETE 200 then re-DELETE 404 → zero leftovers). Bundle: "Edit shift template" + "Shift templates" in the deployed roster chunk.
- **HR Shifts & Roster: bulk COMPLETE / CANCEL / DELETE on assignments (Aug 16)** — the user asked for bulk actions on the `/app/hr/roster` List tab next to the existing bulk-ASSIGN modal:
  - **API** `POST /api/hr/roster/bulk-status` (NEW; static segment beats `[id]`; `withStaff` + `isHrAdmin` — doctor 403): `{ action: "complete"|"cancel"|"delete", ids[] (≤500, deduped) }`. One tenant-scoped fetch of the selected rows, then per-id: **complete** only `scheduled` → `completed` (skips "already completed" / "only scheduled shifts can be marked complete" for missed/cancelled), **cancel** flips any non-cancelled row (incl. completed/missed — mirrors the single-row Cancel button) → skipped "already cancelled", **delete** removes rows (re-run → "not found"). One audit row per call (`entityId: null` — **`audit_logs.entity_id` is a UUID column; a literal `bulk/…` string made logAudit's insert fail SILENTLY (first smoke run left zero audit rows)**; pattern copied from `hr/payroll/bulk`: `changes: {action, count, ids}` + `description "Completed/Cancelled/Deleted N shift assignment(s)"`; action value `delete`→`"delete"`, else `"update"`). Returns `{ action, processed (COUNT — payroll-bulk parity), skipped: [{id, reason}], total }`.
  - **UI** (`hr-roster-view.tsx`, admin-only via existing `isAdmin`): List tab gained a sky **bulk bar** — "Select all {N}" when nothing selected; with a selection: "N selected" + **Complete (N)** (primary), **Cancel (N)** (amber), **Delete (N)** (rose, permanent confirm), Clear + spinner; per-row checkboxes (accent-primary, aria-labelled) before the shift dot; selection pruned via `useEffect` when `rows` reload, cleared + emerald/rose `selMsg` banner after each action. Per-Day/Per-Staff/Per-Week/Per-Month tabs keep their per-row buttons untouched.
  - **Verified**: clean build green; prod restarted on :3000; `temp\opencode\hr-roster-bulk-status-smoke.mjs` **21/21 vs prod** (complete 2 → statuses; idempotent re-complete; missed-row complete skip; cancel on completed+missed flips both; re-cancel skip; delete 4 → rows gone; re-delete "not found"; unknown id skip; bad action/empty ids/>500 DISTINCT ids 400s — the first >500 check used 501× the SAME uuid and deduped to 1 (a lesson); doctor 403; audit "Completed 2"/"Cancelled 2"/"Deleted 4" rows present; zero leftovers — staff_shifts → shifts → staff → users → auth + audit swept by `user_id = qaAdmin.uid` + entity_type staff_shifts/shifts + created_at window). Bundle: `bulk-status` route chunk + "Bulk actions"/"Select all" in the roster SSR chunk.
  - **Smoke gotchas learned**: `audit_logs.entity_id` is UUID — never write non-uuid strings there (silent no-op + `ilike` on the column throws "operator does not exist: uuid ~~* unknown"); sweep audit by `user_id` of the QA fixture, not by entity_id patterns.
- **Users & Roles follow-up (Aug 16, DONE — API GET relaxations landed)**: pages for every guarded module now call `requireModulePage` (`src/lib/module-guard.ts`, server-side: non-null `module_access` is authoritative, `none` → redirect, `view_only` → page renders + passes `accessLevel` down) and pass `accessLevel` into their views; the view masking for **view_only** is landed across the tree (billing-view was the reference; this session finished: wards ×5 [dashboard/admissions/rounds/bed-map/discharges], lab ×3 [dashboard/income/requests+services via existing flags], pharmacy ×7 [view/analytics/inventory-shell/billing/procurement/compliance/BranchAdminTabs], staff-management [New dropdown + import hidden, entire per-card ⋮ action menu hidden, exports stay], plus `components/ui/import-export-menu.tsx` gained `allowImport?: boolean` (default true) — `allowImport={!viewOnly}` keeps Export CSV/PDF while dropping the Import item). **The read-only GET relaxations shipped (Aug 16, this session)** — every list/feed GET now passes through `requireModuleLevel(ctx, key)` instead of an admin-role check, with key = the nav map key: `GET /api/admin/users` + `[id]` → `staff`, `GET /api/audit-logs` → `audit-logs`, `GET /api/hr/permissions` → `hr-staff`, `GET /api/hr/payroll/summary` + `runs` + `runs/[runNumber]` → `hr-payroll` (run detail keeps the own-line-only branch for non-HR callers), `GET /api/subscription` → `subscription`. **Semantics**: NULL `module_access` = role defaults = "full" (so role-default staff can read these too — deliberate per the approved plan; nav never shows those pages to them, but raw API reads are staff-open); explicit `none` in the map → 403 "You do not have access to this module". GETs switched from `withAuth` → `withStaff` where needed (`/api/admin/users` pair — patients still 403 "Staff portal access only"). Writes untouched: POST/PATCH/DELETE admin/users keep role check + `requireModuleLevel(ctx, key, "full")`; PUT hr/permissions + POST hr/payroll stay HR-gated. Verified `temp\opencode\grants-get-relax-smoke.mjs` **27/27 vs prod**: view_only pharmacist 200 on all 5 GETs (+ by-id + matrix rows + summary monthlyTotals + runs list + own-line run detail 404), role-default doctor 200 where relaxed, explicit-none doctor 403 on all 6, patient 403 on staff/HR/subscription feeds, write guards (view_only POST users 403, doctor POST/DELETE users 403, pharmacist PUT hr/permissions 403, pharmacist POST payroll 403), zero leftovers. Note: `GET /api/hr/payroll` list + `GET /api/security-events` remain role-gated (out of the documented scope).
- **HR Shifts & Roster: clinical-staff shift assignment unblocked + Per Month default (Aug 16)** — two user issues on `/app/hr/roster`: (1) the page should land on the **Per Month** tab; (2) Assign shift / Bulk assign refused clinical staff (doctors, medical officers, nurses) with `"Clinical staff member has no verified, unexpired credential"` (single) / silent `CREDENTIAL_REQUIRED` skips (bulk):
  - **Default tab**: the 5-tab rework of `hr-roster-view.tsx` (uncommitted working-copy change) already defaults `useState<TabKey>("month")` — verified in the deployed bundle + SSR (`aria-pressed` on "Per Month") — no change needed.
  - **Migration `0086_hr_roster_no_credential_gate.sql` (APPLIED via CLI)** — `CREATE OR REPLACE FUNCTION hr_assign_shift` with the `staff_credentials` verified/unexpired gate removed (the RPC previously raised `CREDENTIAL_REQUIRED` for the 13 clinical roles). Conflict guards are table-level (UNIQUE staff_id+shift_date, `trg_staff_shifts_conflict` vs duty_roster) and untouched; credential tracking stays on the HR Credentials page — roster assignment is scheduling, not licensure.
  - **API**: `POST /api/hr/roster` dropped its `CREDENTIAL_REQUIRED` → 400 branch (`"Clinical staff member has no verified, unexpired credential"` is gone; SHIFT_CONFLICT + duplicate-key mappings remain); `POST /api/hr/roster/bulk` dropped the `CREDENTIAL_REQUIRED` skip branch (conflict/duplicate "Already assigned on this date" skip stays).
  - **Verified**: build green; migration pushed (remote == local, 0086 listed); prod restarted on :3000; `temp\opencode\hr-roster-clinical-smoke.mjs` **8/8 vs prod** (QA hospital_admin + doctor + nurse with ZERO `staff_credentials` rows: SSR of `/app/hr/roster` contains the Per Month tab; single assign of the credential-less doctor → 201; bulk assign of credential-less admin+nurse → 2 assigned / 0 skipped; re-running bulk for the already-assigned doctor → 0 assigned / 1 skip "Already assigned on this date" (correct — my first smoke expectation wrongly counted the doctor as assignable twice); roster list shows all 3 QA rows; zero leftovers — staff_shifts → shifts → staff → users → auth swept, `qa.ros.*`/`QA-ROS-*` verified empty).
- **Internal mail compose: recipient search bar for staff AND patients (Aug 16)** — the compose **To:** field gained a live search input on top of the existing names dropdown (`mail-view.tsx`): new `toSearch` state + memoized `searchResults` over `recipients.staff` + `recipients.patients` (matches full_name/email/role label case-insensitively, max 20, already-added + `no_path` patients excluded), a results popover (Staff/Patient tag + role label, click or **Enter** to add), chips render below with the original select relabeled "Or pick from the list…" (optgroups Staff / Patients & dependants unchanged, incl. no_path tooltips); broadcast checkboxes clear the search when toggled on; result count 0 → "No matches for …". Build green, prod restarted on :3000, "Search staff or patients by name" confirmed in the deployed client chunk.
- **Users & Roles: per-staff module grants now drive the sidebar nav (Aug 16)** — the user reported that staff such as SAMUEL (Pharmacist) couldn't see modules they'd been granted in Settings → Users & Roles (**Lab, Wards, Billing, Banking, Expenses, Other Income, Staff, Mail, Chats, Medical Reports, Financial Reports**). Root cause: `navForRole` in `frontend/src/lib/nav.ts` filtered by hard-coded per-module role lists (`item.roles`) BEFORE applying the per-user `users.module_access` JSONB — a pharmacist isn't in those modules' default role lists, so the grants were silently dropped at render. Fix (nav-only; the user chose "Nav first, then verify APIs"):
  - **`nav.ts` rewrite**: when `moduleAccess` is non-null it is **authoritative** — `item.roles` and child entry `roles` are skipped entirely, except `ALWAYS_VISIBLE_KEYS` (`account`/`download`/`profile`/`settings`) which stay role-gated as always; `null`/undefined `moduleAccess` keeps the old role-default path byte-for-byte (role-default doctor and role-default pharmacist verified unchanged). Children pruned per key via `accessLevelOf({full,view_only,none})` (full/view_only → visible, none → hidden). Non-null map with a missing key → hidden.
  - **Verified**: clean build green (Next 16 via Turbopack); prod restarted on :3000; `temp\opencode\nav-grants-smoke.mjs` **54/54 vs prod** (SSR HTML assertions on `Get /app/...` responses with session cookies): Samuel-like pharmacist fixture (the user's exact 44-key grant map) sees **Phil's** 17 granted items (Pharmacy, Patients, Appointments, Lab, Reports, Expenses, Other Income, Staff, Banking, Billing, Mail, Chats, Wards, Leave, Financial Reports + dashboard child pages) and hides Audit Logs/Subscription/Settings/Payroll Runs; role-default doctor keeps admin modules, role-default pharmacist keeps only Pharmacy; 4× guard checks; zero leftovers (staff → users mirror → auth all swept, verified `users ilike qa.nav.%@qa.local` + `staff_number ilike QA-NAV-%` = 0 after the run).
  - **Smoke gotchas learned**: (1) two fixtures of the SAME role collide on `uq_staff_number` when `staff_number` is derived from the role title — derive it from a per-fixture key (`staffKey` from the email prefix) instead; (2) the session-cookie map MUST be keyed by fixture name (`emailPrefix ?? role`), not role — the second pharmacist fixture silently overwrote `cookies["pharmacist"]` and the granted pharmacist's SSR checks were then run as the default pharmacist (this one bug burned three failed runs); (3) inline `node -e` scripts mangle under PowerShell quoting — write .mjs files; (4) log delete results in `finally` (`r1.error?.message ?? "ok" + rows`) — a silent users/staff delete failure left 2 users+2 staff after one 54/54 run and was only caught by an external REST leftover check; sweep auth via a poll/retry loop (GoTrue eventual consistency).
  - **Follow-up (completed Aug 16) — view_only GET catalog for the granted pharmacist** (`temp\opencode\nav-view403-probe.mjs`, Samuel-like fixture, all leftovers swept): **every view_only-granted GET returns 200** — dashboard, wards (+dashboard/admissions/discharges-history/ward-rounds/bed-availability), banking (+statement/ledger/bank-accounts), lab-tests/lab-services/lab-orders, hr/dashboard+staff/roster/attendance/shifts/leave-balances/leave-policy, financial/summary (needs `from&to`), expenses, other-income. Correct 403s at the time: `/api/admin/users` (Staff page feed!), `/api/hr/permissions`, `/api/hr/payroll/summary`+`/runs`, `/api/audit-logs`, `/api/subscription` — **all since relaxed (see the API GET relaxations entry above)**. **Over-exposure (minor)**: `GET /api/hr/payroll` list returns 200 even when the map says `hr-payroll: none` (only the summary/runs endpoints are gated). **Bonus bug found+fixed: `GET /api/beds` returned 500 for EVERY role** — the route embedded `ward!inner(...)` (alias unresolvable → PostgREST PGRST200 "Perhaps you meant 'wards'"); fixed to `wards!inner(id, name, ward_type, branch_id, tenant_id)` + `.eq("wards.tenant_id", …)` + `b.wards?.tenant_id` filter, verified 200 with the pharmacist session. 405 `/api/discharges` GET + 404 `/api/hr` are non-issues (pages use `/history`/`[admissionId]` and `/app/hr` respectively).
- **Financial Report: payroll broken down by department (Aug 16)** — follow-up to the module-wise financial report: the user wanted payroll expenses visible per department so the report "captures Ward/Pharmacy/Lab/… payroll" as well as income. No migration (staff.department already exists):
  - **`frontend/src/lib/financial-overview.ts`** — the `payroll_records` query now embeds `staff:staff(department)` (valid: `payroll_records.staff_id REFERENCES staff(id)` — only ONE FK to staff, no hint needed) and computes `payroll.byDepartment: [{department, gross, net, count}]` over the PAID records in the P&L window (gross = base+allowances+bonus+overtime, net = net_salary), sorted net desc, department fallback `"Unassigned"`; interface extended (`byDepartment`). The to-one embed is typed as an ARRAY by PostgREST's codegen — cast `p.staff as unknown as {department?: string} | null` (runtime object), same pattern as the HR payroll embed (`hr-schedules`).
  - **`financial-reports-view.tsx`** — three render sites: (1) Expenses Breakdown panel: new amber sub-block "Payroll by department · N payslips" after the three stream bars (per-dept row: name + mini progress bar vs payroll total + net amount); (2) P&L Statement: "Payroll (net paid)" row followed by indented per-department rows with `<count> payslips`; (3) CSV export + window.print letterhead doc: department lines under the payroll line (`  Dept  amount`), print CSS gained `tr.sub td { padding-left: 24px; ... }` for indents.
  - **Verified**: clean build green; prod restarted on :3000; `temp\opencode\fin-overview-dept-smoke.mjs` **11/11 vs prod** (QA hospital_admin fixture: paid payroll record with department Finance → summary 200, payroll.total 320000, byDepartment array with Finance net 320000/gross 500000/count 1, expenses.total includes payroll, pnl.netCollected; July window excludes the Aug record; bad dates 400; zero leftovers — payroll_records → staff → users → auth sweep confirmed via `fin-overview-leftovers.mjs`). **Smoke gotchas learned**: `staff` has NO `full_name`/`is_active` columns (those live on `users`; `is_available` is the staff flag) — the first two fixture inserts 400'd; the session cookie needs the literal `base64-` prefix: `sb-<ref>-auth-token=base64-<base64url(JSON session)>`.
- **Admin credential management: reset emails + passwords for STAFF and PATIENTS/dependants (Aug 16)** — the user asked for the hospital admin to be able to reset other users' emails and passwords when they forget them. Audit found staff already covered (`PATCH /api/admin/users/[id]` syncs email to auth+mirror; `POST /api/admin/users/[id]/reset-password`); the gaps were all on the patient/dependant side:
  - **Email edits now sync the portal login**: `PUT /api/patients/[id]` and `PUT /api/dependants/[id]` previously only patched the `patients` row — changing a patient's email did nothing to their sign-in. Both now normalize/validate the email (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`, trimmed lowercase) and, **when the record has a `user_id`**, call the new shared `syncPortalAccountEmail(svc, userId, email)` in `lib/dependant-portal.ts` (auth `admin.updateUserById {email, email_confirm: true}` FIRST so a duplicate fails before anything is touched, then the users-mirror row) — same authoritative-email pattern as the admin-users PATCH. Login-less patients/dependants are untouched (no `user_id` → plain row update, no auth). Invalid email → 400; duplicate email (already used by another login in auth) → 400 before the mirror is written. Audit description gains `" (portal login email synced)"`.
  - **Password reset UI for accounts that ALREADY exist**: the provision-portal routes always supported `forceReset` (rolls a new temp password on an existing account) but the UI only rendered the button for login-less users. `patient-dialog.tsx`:
    - Primary patient action row: single amber `KeyRound` button that reads **"Create portal login"** when `!detail.user_id` and **"Reset password"** when `detail.user_id` is set (both conditions admin-only via `canDelete` + `status === "active"`), calling `provisionPrimaryLogin(!!detail.user_id)` → `POST /api/patients/[id]/provision-portal` with `{forceReset}`. Force-reset confirm warns "the old one stops working immediately"; depInfo message becomes "Portal password reset for … email …, temporary password …".
    - Dependant card action row: `{!d.user_id ? "Create portal login" : "Reset password"}` via `provisionDependantLogin(d, !!d.user_id)` → `POST /api/dependants/provision-portal` with `{patientIds: [id], forceReset}`. Same confirms/depInfo split.
  - **Verified**: clean build green (deleted stale `.next` first — the earlier collision-lock lesson), prod restarted on :3000 (note: `Start-Process node "node_modules\next\dist\bin\next" start -p 3000` FAILS — `-p` is ambiguous to Start-Process; must use `Start-Process node -ArgumentList "node_modules\next\dist\bin\next","start","-p","3000"`); `temp\opencode\credential-reset-smoke.mjs` **33/33 vs prod**: primary email PUT syncs `patients.email` + users mirror + auth email, sign-in with the NEW email works / OLD email fails; dependant email PUT syncs all three; primary + dependant forceReset each return a temp password, old password dead, new password signs in; doctor 403 on provision-portal; invalid email 400; duplicate email 400 (no partial write); login-less patient email edit saved with no auth/mirror created; ≥2 `"portal login email synced"` audit rows; zero leftovers (users mirrors → auth → patients → audit `%QA-CRED%` swept + hard per-id auth sweep loop). Regression `provision-primary-smoke.mjs` still **28/28** (provides non-forceReset path unchanged). Bundle: `server/chunks/_1vvfb5c._.js` holds "portal login email synced"; SSR `patient-dialog` chunk holds "Reset password".
- **Primary patient "Create portal login" (provision-portal) button (Aug 16)** — closes the gap where a login-less PRIMARY patient added via CSV import could never get a portal account in the UI (only dependants had provision-portal; the Add-patient "Patient portal login" checkbox only applied at registration). Now:
  - **`POST /api/patients/[id]/provision-portal`** (NEW route, `withAuth` + `isAdminRole` only; mirrors `dependants/provision-portal`): loads the tenant-scoped patient; rejects non-primary (`400 "Only primary patients can be provisioned here — use the dependant flow instead"`), non-active (`400`), unknown (`404`); already-has-account → `200 { provisioned: [], skipped: [{reason: "already has a portal account"}] }` unless `forceReset: true` (rolls a new password on the existing auth account); requires `patients.email` (else skip "no email on file"); otherwise `createPortalAccount` (auth `patient_api` + users mirror row) linked to `patients.user_id = userId` with rollback (delete auth + users mirror) on link failure; `logAudit "Provisioned portal login for primary patient <patient_number>"`; returns `{ provisioned: [{patientId, patientNumber, firstName, lastName, email, tempPassword}], skipped }`. **Param gotcha: the id is the SECOND-TO-LAST segment** (`segs[segs.length - 2]`) because `provision-portal` is a static segment after `[id]` — using `.pop()` yields the string `"provision-portal"` and PostgREST 400s `invalid input syntax for type uuid` (caught by the first smoke run).
  - **UI** (`patient-dialog.tsx`): the primary patient's detail-modal action row now shows an amber **"Create portal login"** `KeyRound` button (admin-only via `canDelete`, hidden when `detail.user_id` is set or status ≠ active), confirming then calling `POST /api/patients/[id]/provision-portal`, showing the one-time temp password in the `depInfo` banner. **The `depInfo` banner previously rendered ONLY inside the edit form** — added it to the non-edit Patient Info view too so the provisioned-credentials message is visible right where the button lives (same amber status styling as the dependant flow). After success the modal reloads + `router.refresh()` so the new portal account shows in the Patients list (and mail recipients list picks the patient up as a real portal user).
  - **Verified**: build green (Next 16 via Turbopack, one build EXITCODE=0 after earlier parallel-build collisions left a stale `.next` lock — "previous build didn't exit cleanly"); prod restarted on :3000; `temp\opencode\provision-primary-smoke.mjs` **28/28 vs prod**: doctor 403, unknown patient 404, inactive 400, dependant 400 (use-dependant-flow), success 200 + temp password, `patients.user_id` linked, users mirror `patient_api` + tenant, auth account with app_metadata, re-provision skipped already-has (no duplicate), forceReset rolls new password, provisioned primary now appears in `/api/mail/recipients` with `has_account` (no longer `no_path`) and mail to it delivers to the new portal user id — **smoke note: a patient WITH a login is addressed by its USER id, not patient id, in the mail send (`recipientIds: [linkedUserId]`); patient ids only resolve when login-less** (my first attempt sent `qaPatientId` → 400 "No valid recipients", but that's correct compositor behavior); zero leftovers (fixture patients + users mirrors + auth all removed; audit `%Provisioned portal login for primary patient QA-PP-%` swept). **Smoke/cleanup gotchas**: `SVC.from(...).delete().then(() => {}, () => {})` on the supabase-js thenable — `.catch()` is NOT a function there (FATAL mid-cleanup once); `SVC.auth.admin.deleteUser(...)` can silently fail when swallowed by `.then(()=>{},()=>{})` AND GoTrue `auth.admin.listUsers` is eventually-consistent (returns stale rows right after deletes) — the leftover check must poll/retry (loop up to ~15s) instead of asserting a single read; a hard **per-id auth sweep loop** (list → filter `startswith("qa.pportal")` → `await deleteUser(id)` capturing `{error}`, repeat until 0) is the reliable cleanup (accumulated 5 strays from crashed runs across multiple earlier runs were purged this way; the earlier "0" verify was a stale read). Bundle: new route chunk `server/app/api/patients/[id]/provision-portal/route_actions_*.js` + "Create portal login" string in the patient-dialog static chunk.
  - **Resolution guidance for the originally-reported no-path primaries (PT-0012 Ada Okafor / PT-0013 Isak Ibrahim / PT-0014 David Fashina)** — two paths going forward: (1) add a dependant WITH a portal login to each primary → mail resolves via family (like PT-0015 Mojisola → Taiwo Mafe); (2) use the new **Create portal login** button in the patient detail modal → they get their own portal account, appear in mail recipients as normal patients, and can sign in at `/login`. Both require each patient to have an email on file.

### Completed
- **Internal mail compose: ALL patients/family listed + login-less primary resolution (Aug 15/16)** — follow-up to the patients-broadcast work: the compose recipient picker previously listed only patients WITH portal logins, so the tenant's login-less PRIMARY patients (no account) were un-selectable even when a logged-in dependant could still receive the mail. Now:
  - **`GET /api/mail/recipients`** appends ALL active login-less patients. (a) **Login-less dependants** (unchanged branch) list with `delivered_via: "<primary holder>"` and `has_account: false`. (b) **NEW: login-less PRIMARY patients** — listed with `is_primary: true`, and `delivered_via: "<first family member (dependant) with a portal login>"` when one exists; when the primary has NO logged-in family member → `no_path: true` (+ null via) so the UI flags "cannot receive mail". The user-group branch (`role = patient_api` full OR rollup) is unchanged — per-patient delivery resolution happens at SEND time.
  - **`POST /api/mail/inbox`** — after the existing login-less-dependant → primary-holder resolution, a NEW branch resolves **login-less primary ids → their family members (dependants) with portal logins** (`.eq("is_primary_account", true).is("user_id", null).in("id", leftover)` then dependants `.not("user_id","is",null).in("primary_account_id", primIds)` → push those user_ids, self-excluded, deduped). So mail to a primary lands in the logged-in dependant's portal inbox (mirrors the dependant side). `no_path` patients alone → recipientIds empty → `400 "No valid recipients"` (the UI disables them, but the guard is real).
  - **UI** (`mail-view.tsx`): `RecipientOption` gained `no_path?: boolean`; `recipientLabel` renders `"<name> (no portal account — cannot receive mail)"`; patient `<option>`s disable when `to.includes(r.id) || r.no_path` with a "No portal account — mail cannot be delivered" title tooltip (preserves multi-select convenience: previously a lone no-path selection sent the message into a 400).
  - **Verified**: build green (Next 16 via Turbopack); prod restarted on :3000; `temp\opencode\mail-recipients-smoke.mjs` **26/26 vs prod** (16 real patients listed: 12 with portal logins + 3 no-path primaries Ada Okafor PT-0012 / Isak Ibrahim PT-0013 / David Fashina PT-0014 + 1 via-family Mojisola Mafe PT-0015 → delivered_via "Taiwo Mafe"; send to no-path alone → 400; send to Mojisola → exactly 1 recipient = Taiwo's portal user_id; mixed send (no-path + login patient) → delivered only to the login patient; patient caller sees staff only / patients group empty; staff group excludes the admin sender (self-exclusion) yet is populated; zero leftovers — QA admin+patient users/auth + internal_messages/recipients + 4 audit rows (`%QA via-family%` / `%QA mix%` / `%QA no-path%`, deleted one-by-one by id) removed). **Smoke gotchas**: the QA patient fixture (a `patient_api` user with no patients row) counts as a 17th portal patient — subtract it (`pts.filter(x => x.id !== pUser.id)`) before expecting 16; the admin IS self-excluded from the staff group AND patients group — assert absence, not presence; the recipients route SKIPS login-less dependants whose primary holder has no login (`!prim?.user_id → continue`), so a no-login primary with a no-login dependant still shows as `no_path`; the smoke's cleanup deletes internal_messages BUT the `logAudit` rows survive — sweep them by id afterwards (a chained `.ilike().or()` quietly no-ops on the service client; delete per-id).

### Completed
- **Internal mail: patient broadcast + individual patient/dependant recipients + collapsible sent messages (Aug 15)** — the user asked for (1) a broadcast tick box to all Patients & their dependants like the staff one, (2) sending to individual Patients/Dependants like staff, (3) collapsible sent messages:
  - **Migration `0084_internal_mail_broadcast_scope_patients.sql` (APPLIED via CLI)** — `internal_messages.broadcast_scope` CHECK extended `('staff','all')` → `('staff','patients','all')` (drop/re-add; a pre-migration smoke POST 400'd on `internal_messages_broadcast_scope_check` — first scope-extension attempt must push the migration BEFORE testing).
  - **API** `POST /api/mail/inbox` (staff senders): `broadcastScope: "patients"` → recipients = ALL tenant `users.role = patient_api` (patients + dependants-with-portal-login — dependants without an account can't receive mail anyway); `"all"` unchanged (staff + patients); sender always self-excluded. Individual sends already accepted patient ids when the sender is staff (the `isPatient` neq-patient filter is sender-scoped) — no change needed. Patient senders stay blocked from broadcast (400).
  - **UI** `mail-view.tsx`: compose gained a second broadcast checkbox "Broadcast to all patients" (helper "Every patient (and their dependants with portal access) receives the message"); both ticked → `broadcastScope: "all"`; recipient picker now loads `GET /api/mail/recipients` `{staff, patients}` groups (was `staff ?? []` only) and renders staff under `<optgroup label="Staff">` and patients under "Patients & dependants" so staff can pick a specific patient/dependant; chips + lookup merged over both groups. **Collapse**: the message-row click now TOGGLES (was open-only — sent messages could never be collapsed after reading); a ▾ indicator (rotates 180° when open) shows on sent rows; the expanded sent message's "To:" line now renders scope-aware summaries ("Broadcast to all patients · N recipient(s)" / "…all staff · N" / "…staff & patients · N") instead of listing hundreds of names.
  - **Verified**: build green; prod restarted on :3000; migration pushed; `mail-broadcast-smoke.mjs` **21/21 vs prod** (recipients groups: QA patients listed; broadcast patients → ONLY the 3 QA patient_api accounts + real tenant patients (sender admin + doctor excluded, scope stored `patients`); broadcast all → staff+patients reachable, sender excluded; individual patient send → exactly 1 recipient; patient inbox sees both broadcasts; sent list carries recipients; patient broadcast 400; zero leftovers — QA messages purged by subject prefix, patients→users→auth then audit sweep). Smoke gotchas: sender is always self-excluded from its own broadcast (don't assert admin ∈ recipients of its own broadcast-all); a patients-broadcast hits EVERY tenant patient account (assert ≥ QA count + includes, NOT exact count); cleanup `.or()` must be a single combined expression. Bundle: "Broadcast to all patients", "Patients & dependants", "dependants with portal access" in the mail-view chunk.

### Completed
- **HR staff CSV import full profile + edit-form employment type (Aug 15)** — the user asked for (1) the Staff edit form to cover Employment/Base salary/Hire date/Grade/Bank/Account name/Account number, (2) a full import template + import of "all the information needed at once", and (3) payroll-config (structure, rent, internal deductions, pension pin, tax id) editable via the edit form:
  - **POST /api/hr/staff reworked into a full-profile upsert** — `{staff_id (required), employment_type, base_salary, hire_date, salary_grade, bank_name, bank_account_name, bank_account_number, pensionable_portion_pct, pension_rate_pct, nhis_applicable, nhf_applicable, basic/housing/transport/utilities/meals/others pcts, annual_rent, annual_mortgage_interest, annual_life_assurance, internal_deductions, pension_pin, nhf_number, tax_id}`. Only **present non-empty keys** are applied (`txt(v) !== ""` gate per key) — re-imports update in place and LEAVE BLANK CELLS ALONE (no clobbering: bank/rent survive a re-import that only sends pension_rate_pct + internal_deductions). Money cells strip `₦`/commas (`money()`, negative → 400); pcts must be ints 0–100; booleans accept `true/false/yes/no/on/off/1/0` **case-insensitively** (Excel exports `FALSE` — a real import failed 18/18 until `bool()` lowercased; `true`/`yes`/`yes` slash dates `3/1/2026` normalised to ISO via `parseDate` (MM/DD/YYYY); a numeric-only `internal_deductions` cell (`0`) → `[]` (no deductions) instead of a bogus `{description: "0"}` row); `internal_deductions` accepts the CSV string form `"Desc:Amount;Desc:Amount"` or an array (objects `{description, amount}` or `"Desc:Amount"` strings, zero-amount rows with empty desc dropped). Upserts staff (employment_type/base_salary via a second update) + `staff_profiles` (`onConflict: "staff_id"`); returns `ok({...data, created: wasCreated}, 201)` so the client can count created vs updated. Audit "Imported HR profile for <id>".
  - **PUT /api/hr/staff/[id] made presence-based (behaviour change)** — the old route `.update()`d a profilePatch with **every field defaulted to null**, so a PUT sending only `{employment_type}` wiped hire_date/bank/grade (smoke caught: DB after section 4 showed `bank_name: null, hire_date: null`). Now only keys present in the body are patched (`if (body?.x !== undefined)`), explicit `null`/`""` still clears. Route also gained `employment_type` (staff-table field; empty → 400).
  - **UI** `hr-staff-view.tsx`: (1) edit modal added an **Employment type** select (full_time/part_time/contract/locum, prepopulated from the detail row, sent as `employment_type` on save); (2) import template expanded from 6 → **25 columns** (`staff_id, employment_type, base_salary, hire_date, salary_grade, bank_name, bank_account_name, bank_account_number, pensionable_portion_pct, pension_rate_pct, nhis_applicable, nhf_applicable, basic_salary_pct, housing_pct, transport_pct, utilities_pct, meals_pct, others_pct, annual_rent, annual_mortgage_interest, annual_life_assurance, internal_deductions, pension_pin, nhf_number, tax_id`) with a richer sample row (GTBank/GL 08/₦200,000/80/8/false/true/…/«Staff housing:25000;Loan:10000»/PENCOM PIN/NHF no/TIN); the importer sends only non-blank cells and reports "N existing profile(s) updated with the imported values"; (3) export CSV gained `staff_id` + bank account name/number (25→16 cols; staff_id first so exported files round-trip into the import). The payroll-settings edit grid was ALREADY in the detail modal (batch "PAYROLL module ported", Aug 15) — nothing to add for part 3.
  - **Verified**: build green; prod restarted on :3000 (the first re-run after the PUT fix FAILED until restart — stale prod was still running the old null-everything code); `hr-import-smoke.mjs` **56/56 vs prod** (full 25-key POST + created=true + every field persisted incl. internal deductions 2 rows and pins; re-import created=false + pension 11 + bank/rent untouched + deductions replaced; guards: pct>100, bad bool, negative money, missing/unknown staff_id 400s, cashier 403; PUT employment_type persists (contract) + empty 400 + profile WRITE-SURVIVES (the presence fix); GET list embed incl. bank_account_number; zero leftovers: staff_profiles→staff→users→auth + audit `%Imported HR profile%`/`%Updated HR profile%` swept). `hr-policy-smoke.mjs` regression 35/35 (PUT presence change is behaviour-compatible — the old smoke sends explicit null/"" clears). Bundle: "Employment type", "Pensionable base %", "hr-staff-import-template", "existing profile(s) updated" in chunks.
  - **Smoke gotchas learned**: crashed smokes leave auth + users mirrors (first run died on `uq_staff_number` — TWO QA users shared one `QA-<ts>` staff_number → suffix the number per user); purge via `SVC.auth.admin.listUsers()` email filter; **the stale prod trap**: after a route change, ALWAYS kill+restart :3000 before re-running smokes — a green build does not load new route code into a running server.

### Completed
- **PAYROLL module ported from SkyBooks (Aug 15)** — the user asked to use "the process and procedures, formats and functions" of the SkyBooks PAYROLL module (reference: `C:\Users\Admin\Downloads\skybooks\src\services\payroll.service.ts` 721 lines `calculatePayrollForEmployee`, `src\routes\payroll.ts`, `src\pages\payroll\PayrollRunsPage.tsx`, schema in `src\db\tenant\tables.ts` ~line 1303). Confirmed decisions: **full port**, **Naira 2dp** (SkyBooks used integer kobo — same maths, r2 rounding), **Approve=freeze, Pay=ledger** (SkyCare has no GL chart of accounts — paid posts a `hospital_bank_ledger` 'out' row instead of double-entry):
  - **Migration `0082_hr_payroll_skybooks_port.sql` (APPLIED via CLI)** — `staff_profiles` gains the SkyBooks salary-struct columns: `pensionable_portion_pct` 80, `pension_rate_pct` 8, `nhis_applicable` false, `nhf_applicable` true, struct pcts `basic/housing/transport/utilities/meals/others` 50/20/10/10/5/5, `annual_rent`/`annual_mortgage_interest`/`annual_life_assurance` (naira), `internal_deductions jsonb '[]'`, `pension_pin`/`nhf_number`/`tax_id` text, `chk_sp_pct_range` CHECK (each pct 0–100). `payroll_records` gains run metadata + statutory columns: `run_number text`, `pay_date date`, `paye`, `pension_ee`, `pension_employer`, `nhf`, `nhis`, `nhis_employer`, `other_deductions` (= nhis+nhf+internal), `internal_deductions_total`, `tax_relief`, `annual_gross`, `chargeable_income`, `effective_rate_pct numeric(6,2)`, `calc jsonb '{}'` (full engine snapshot incl. `bandBreakdown`) + `idx_payroll_run_number`/`idx_payroll_pay_date`. `0083_hr_payroll_updated_at.sql` (APPLIED) — **bug fix**: 0059 attached `trg_payroll_records_updated_at` (`update_timestamp()` sets `NEW.updated_at`) but the table was defined WITHOUT `updated_at` → every UPDATE on payroll_records (re-run rebuild, approve, adjust) failed with `record "new" has no field "updated_at"`. 0083 adds `updated_at timestamptz NOT NULL DEFAULT now()`.
  - **Engine** `frontend/src/lib/hr-payroll-calc.ts` — `calculateHrPayroll(gross, cfg)`, `DEFAULT_HR_PAYROLL_CONFIG`. SkyBooks math verified line-by-line: struct pcts sum-normalised; basic/housing/transport/utilities/meals/others from gross; pensionable = gross × 80%, pension **EE = rate% of pensionable (default 8%), ER = 10%**; NHIS EE 5% / ER 10% of basic (only if `nhis_applicable`); NHF 2.5% of basic (unless off); annualise → reliefs: rent = `min(20% annual rent, 500k)` else **`min(4% annual gross, 500k)`** (SkyBooks caps the fallback too — the smoke's first refCalc had it uncapped and rightly failed), mortgage/life each `min(value, annualGross)`; PAYE bands (cumulative limits, width math): 0% first 800k, 15% next 2.2m, 18% next 9m, 21% next 13m, 23% next 25m, 25% above; `monthlyPAYE = annualPAYE/12`; `net = gross − pensionEE − nhis − nhf − monthlyPAYE − internalTot`.
  - **API**: `POST|GET /api/hr/payroll` (POST = `withStaff` + `isHrAdmin`/`hr.payroll.run`; body `{period, payDate?, staffIds? (≤200)}`; **active-staff only via `users` id-list resolution** — `.eq("users.is_active", true)` on the embed would be silently ignored (to-one embed landmine); **run numbering = `PR-` + distinct run_number count + 1, pad 4 — reruns of an existing period PRESERVE the existing run_number** (a first version re-counted ROW count not distinct values and renumbered the rebuild to PR-0003/PR-0005); payDate defaults to last day of period; rebuilds draft records in place (paid period → 400 "already paid — cannot re-run"); inserts `payroll_lines` (basic/housing/transport/utilities/meals/other allowances + NHIS/NHF/Pension (employee)/PAYE tax + `Internal: <desc>` lines); summary `{period, payDate, runNumber, generated, updated, staffCount, total_gross/paye/pension/nhf/nhis/net}` + audit "Ran payroll for <period> (PR-…)").
  - `PUT|GET /api/hr/payroll/[id]` — GET payslip (record incl. `calc` + lines; HR admin or own staff). PUT status flow: verbs `draft|approved|paid`; approve requires ≥1 line + sets `approved_by`; **paid requires approved — but idempotent re-mark paid allowed** (the first guard `rec.status !== "approved"` 400'd the UI's payment retry); paid→draft 400; **adjustments only in draft** (allowances/bonus/deductions; frozen modes 400). **Adjust-net gotcha**: the legacy formula `base + allowances + overtime + bonus − deductions` is nonsense on engine records (base_salary is now GROSS and deductions holds only nhis+nhf+internal) — recompute is a **delta over `calc.netPay`**: `calcNet + (allowances−stored) + (bonus−stored) − (deductions−stored) + overtime`. `status=paid` posts a `hospital_bank_ledger` 'out' row (source `payroll`, `payroll_id` idempotency guard, first active bank else cash via `resolveBankAccountId`/`bankLedgerAccountForMethod`, reference `PR-<run_number pad 4>`, sourceRef "Payroll for <period>"). PUT returns `{id, net_salary, status}`. Audit both.
  - `GET /api/hr/payroll/summary?year=YYYY` (NEW — static `summary` segment must stay, it wins over `[id]`) — `{year, monthlyTotals: [12 × {month, monthName, gross, paye, pension, pension_employer, nhf, nhis, net, count}], annualTotals}`; **approved+paid only** (drafts excluded — PAYE-return oriented); gross = base + allowances + bonus + overtime; month from pay_date (fallback generated_at).
  - `PUT /api/hr/staff/[id]` — extended with payroll-config fields (pcts 0–100, naira ≥0 with `null`/`""` clearing to 0, `internal_deductions` array filtered/mapped, pins as text-or-null), audit — the same route now serves HR-profile + payroll-config PUTs.
  - **UI** `hr-payroll-view.tsx` — SkyBooks-style rework: Run modal (pay date defaulting to period end + staff multiselect w/ search, default all active), 6 totals cards (Records/Gross/PAYE/Pension EE/NHF/Net payable), per-row Approve | Adjust | Payslip | Mark paid | Unapprove, payslip modal (earnings + deductions lines + **tax-computation table from `calc.bandBreakdown`** + chargeable/annual/relief/effective rate), window.print payslip, recharts summary BarChart (Gross vs Net) + annual chips, CSV export (23 columns). `hr-staff-view.tsx` detail modal gained an admin-only **"Payroll settings"** section (Edit grid: pcts, NHIS/NHF toggles, rent/mortgage/life, PENCOM PIN/NHF no/TIN, dynamic internal-deductions rows + read-only summary card when collapsed; saves via staff PUT then refreshes).
  - **Verified**: clean build green; migrations pushed; prod restarted on :3000; `hr-payroll-skybooks-smoke.mjs` **75/75 vs prod** (engine vectors 2.5M doctor / 1.8M nurse against inline refCalc — PAYE 432,262.50, relief 500k cap, 5-band snapshot with last 23% @2,205,000, chargeable/effective rate; PR-XXXX + rerun-number preservation; 9 lines; approve→freeze→unapprove→adjust net=calcNet+bonus→re-approve; paid→ledger + amount + idempotent re-PUT; paid→draft 400; paid-period re-run 400; payslip; summary Jan 2 / Feb 0 draft-excluded + annual; staff config PUT persisted + engine picks it up (11% pension, NHIS, rent 240k relief, internal 25k + line); guards cashier 403 + 4×400s; zero leftovers — audit `%payroll%` sweep MUST run BEFORE users-mirror deletes). `hr-bulk-smoke.mjs` regression 39/39. Bundle: "Run payroll", "Net payable", "Mark paid", "Payroll summary", "Tax computation", "Chargeable income", "Tax bands", "Pay date", "Payroll settings", "Internal deduction" all in chunks.
  - **Smoke gotchas learned**: POST summary uses camel `runNumber` (records use snake `run_number`); PUT returns `{id, net_salary, status}` — assert via `data.status`; list GET RECORD_SELECT includes `calc` (band checks); PostgREST counts ROWS — distinct run numbers via `new Set(...)` client-side; crashed smokes leave auth accounts + users mirrors (cleanup crashed before user deletion) — purge via `SVC.auth.admin.listUsers()` email filter + users-mirror deletes (standalone once staff/payroll/audit rows are gone).
- **Payroll runs index + run detail + PAYE/Pension/Schedules + Payslips pages (Aug 15)** — SkyBooks-style payroll runs ("PayrollRunsPage"): every payroll batch is now a first-class **run** (one `run_number` shared by all records of a batch) with derived status + aggregate totals, feeding three statutory schedule views and a payslip archive:
  - **API**: `GET /api/hr/payroll/runs` (HR admin/`hr.payroll.view` only) — groups `payroll_records` by `run_number` (legacy null → `LEGACY-<period>`), orders period desc, returns `{runNumber, period, payDate (first non-null), status, staffCount, gross/base_salary sum, paye, pensionEE, pensionER, nhf, nhis, net}`; **status derivation = allPaid→paid, anyDraft→draft, else approved**. `GET /api/hr/payroll/runs/[runNumber]` — full RUN_LINE_SELECT (all statutory cols + `calc` + `staff:staff(staff_number, department, specialization, users(full_name, role, email), profiles(bank_name, bank_account_number, pension_pin, nhf_number, tax_id))`); HR admin sees all lines, **non-admin staff see ONLY their own line** (staff_id filtered; zero rows → 404), unknown run → 404; returns `{run: {runNumber, period, payDate, status, staffCount}, lines}` sorted net desc.
  - **Run-number bug fixed (found by smoke)**: POST `/api/hr/payroll` previously called `nextRunNumber()` **per record**, so a 3-staff batch became PR-0001/PR-0002/PR-0003 (each record its own "run") — the runs index showed 3 × staffCount-1 groups and totals never matched. Fixed: a fresh period gets ONE shared `batchRunNumber()` for the whole POST; new records in a period that already has a `run_number` (rerun adding staff) JOIN it (`periodRunNumber` lookup) instead of minting a new one; existing records keep their own number on rebuild. Only a genuinely new run consumes a sequence number (`batchRun ?? nextRunNumber()`).
  - **UI**: nav gained 4 Payroll children (`HR_FINANCE` roles): "Payroll Runs" (`/app/hr/payroll`, existing view = the run modal / records grid), **"PAYE Schedule"** (`hr-paye-schedule-view.tsx` — run picker, per-line chargeable/annual-gross/relief/PAYE/effective-rate table, CSV, letterhead print via `printScheduleDoc`, bulk-delete drafts), **"Pension Schedule"** (`hr-pension-schedule-view.tsx` — EE/ER/NHF columns + totals, bulk-delete drafts), **"Payslips"** (`hr-payslips-view.tsx` — run picker, employee list, per-payslip `payslipPrintHtml` letterhead doc). Shared lib `frontend/src/lib/hr-schedules.ts` (`HrRun`/`HrRunLine` types, `fetchRuns`/`fetchRunDetail`/`bulkDeleteLines`, `calcOf`/`chargeableOf` incl. legacy fallback `annualGross − relief − pensionEE×12 − nhf×12`, `printScheduleDoc` letterhead table doc, `payslipPrintHtml` SkyBooks-style payslip with earnings/statutory/employer-contribution cards + band table + tax-computation + payment info + annual metrics). Page files: `src/app/app/hr/payroll/paye-schedule|pension-schedule|payslips/page.tsx`.
  - **Verified**: `hr-schedules-smoke.mjs` **58/58 vs prod** (3-staff 2029-07 batch: one PR run + run meta payDate 2029-07-31 + totals vs refCalc sums, run detail lines 3 + staff embed + doctor engine figures incl. pensionER 200k / chargeable 27,205,000 / relief 500k, cashier 403 on index but **200 own-only 1 line** on detail, receptionist (not in run) 404, unknown run 404, draft-run line delete → staffCount 2 + totals shrink, status ladder draft→approved→approved-mixed→paid, delete-paid-line skipped, zero leftovers incl. ledger payroll rows + 6 audit sweep + auth purge) + `hr-payroll-skybooks-smoke.mjs` regression **75/75** (run-number change is behavior-compatible). Bundle: "PAYE Schedule"/"Pension Schedule"/"Payslips"/"Chargeable income"/"Annual gross" in chunks. **Build gotcha (same day)**: Google rotated the Figtree v9 woff2 hashes mid-version — `next/font/google` started 404ing (`Can't resolve '@vercel/turbopack-next/internal/font/google/font'`) and **every build failed**; fixed by self-hosting: downloaded the current latin + latin-ext **variable** woff2s into `frontend/src/app/fonts/figtree-latin.woff2` + `figtree-latinext.woff2` and switched `layout.tsx` to `localFont({src: [{path:…, weight: "400 700"}], variable: "--font-heading"})` — Inter/Noto_Sans stay on google. No visual change (same family, variable range covers 400–700).
- **Bulk payroll actions: approve / delete / edit selected drafts (Aug 15)** — user requested bulk approve, bulk delete and bulk edit on the Payroll page for drafted records:
  - **API** `POST /api/hr/payroll/bulk` (static `bulk` segment wins over `[id]`, same pattern as `summary`; `withStaff` + `isHrAdmin`) — body `{action: approve|delete|edit, ids[] (≤500, deduped), payload?}`; one tenant-scoped fetch of all records up-front, then per-id loop. **approve**: drafts only → `status approved` + `approved_by` (re-runs → skipped `already approved`), requires ≥1 `payroll_lines` row per record (line-less → skipped `no payroll lines — run payroll first`). **delete**: drafts only → record row removed, `payroll_lines` gone via FK cascade (approved → skipped `approved — unapprove before deleting`, paid → skipped `paid run — cannot be processed`). **edit**: drafts only → `payload` fields (allowances/bonus/deductions/overtime_pay positive numbers, notes nullable) applied per record with the SAME delta-over-`calc.netPay` semantics as the single PUT (`calcNet + (new−stored)…`); empty payload or any negative → 400; approved/paid records skipped. Response `{action, processed, skipped: [{id, reason}], errors}`. One audit row per call (`Bulk-approved/deleted/edited N payroll record(s)`, changes carry `{action, count, ids}`) — note past-tense map, `Bulk-${action}d` renders "Bulk-editd"). Unknown ids → skipped `not found`.
  - **UI** `hr-payroll-view.tsx`: admin-only checkbox column (header = select all drafts / toggle; per-row checkbox disabled for non-draft with title tooltip) + sky bulk bar when `selected.length > 0` showing "N of M selected — draft records only" with **Approve (N)** (confirm: freezes records), **Edit (N)** (modal: Allowances/Bonus/Deductions/Overtime/Notes all optional, "Leave blank to keep", submit requires ≥1 field, "Apply to N record(s)"), **Delete (N)** (confirm: permanent, removes records + lines) and Clear; amber skipped-reasons banner + emerald result banner; selection pruned on reload; colSpan 7→8 (admins).
  - **Verified**: build green (one Turbopack parse error fixed: `??` mixed with `||` needs parens), prod :3000, `hr-payroll-bulk-smoke.mjs` **55/55 vs prod** (5 drafts via 5 QA staff: approve 2 + approved_by, re-approve idempotent skips, mixed with unknown id, delete approved skipped + record survives, delete draft processed + record/lines gone, edit mixed processed 1 + applied + net = calcNet + (patch−stored) delta verified twice incl. deductions/overtime, approved record untouched, negative/empty-payload 400s, line-less approve skipped, paid record delete+approve skipped + ledger row intact, cashier 403, bad action/empty ids/>500 ids 400s, 4 distinct audit rows with id arrays, zero leftovers) + `hr-payroll-skybooks-smoke.mjs` regression **75/75**. Bundle: "draft records only", "Leave blank to keep", "Apply to" in built chunks. Smoke lesson: net-delta expectations MUST subtract stored values (`calcNet + (new − stored)`) — "calc net + bonus + allowances" naively is wrong when the record already carries allowances.
- **HR module batch 2 (Aug 14/15)** — four user issues on the HR suite:
  - **Leave entitlements now configurable per year** — migration **`0081_hr_leave_type_policy.sql`** (APPLIED via CLI, remote == local): new `hr_leave_type_policy(tenant_id, leave_type PK, entitled_days numeric(5,1) CHECK 0..365, updated_by → users, updated_at; RLS staff-read)` seeded from tenants with defaults **annual 21 / sick 10 / emergency 3 / study 5 / maternity 60 / paternity 5 / unpaid 0**; `staff_leave` + `leave_balances` CHECKs extended to include `'emergency','paternity'`; `hr_sync_leave_balances` rewritten policy-aware (COALESCE policy defaults) with `ON CONFLICT DO UPDATE entitled_days`. New API `GET|PUT /api/hr/leave-policy` (GET → rows; PUT → HR admin only via `isHrAdmin`, per-type validation 0–365, upsert on `tenant_id,leave_type`, then RPC `hr_sync_leave_balances`, audit "Updated annual leave entitlements: annual=25, …", returns `{ rows, synced: true }`). `leave-view.tsx` `LEAVE_LABELS` gained emergency "Emergency" + paternity "Paternity".
  - **HR Dashboard redesign** — `hr-dashboard-view.tsx` fully rewritten (recharts `^3.10.1`: hero gradient + greeting-by-time-of-day, KPI cards, attendance donut, department bars, leave balance donut + per-type progress bars, shift coverage ring, 14-day attendance pulse line from `GET /api/hr/attendance?month=YYYY-MM`, credential compliance alerts, staff table, **admin-only "Annual leave entitlements" card** — inline days editor → PUT policy → re-sync + reload; isAdmin from `/api/auth/me` claims.role).
  - **Staff profile edit save bug fixed** (user report: grade/hire date never saved, modal showed empty values): root cause = (a) `PUT /api/hr/staff/[id]` only `.update()`d `staff_profiles` and 404'd when the profile row was missing, and (b) **`profiles:staff_profiles(*)` is a 1:1 (!) embed — PostgREST returns a single OBJECT (or null), never an array** — the UI read `profiles?.[0]` everywhere so Hire date/Grade/Bank always rendered "—". Fixed: route now upserts `staff_profiles` onConflict `staff_id` (creates when missing, TDZ `staffErr` destructure bug fixed), accepts `baseSalary` (≥0 → `staff.base_salary`; `null`/absent = leave unchanged **by design**), audits "Updated HR profile"; frontend switched ALL reads to object access (`r.profiles?.hire_date`; types `profiles: {...} | null`), edit form gained **Base salary (₦)** input (per-field diff on save), success banner "Profile saved ✓".
  - **Roster duplication resolved** — legacy `/app/roster` (duty_roster) now redirects to `/app/hr/roster` (`src/app/app/roster/page.tsx` → `redirect()`; nav `roster` item removed); duty-roster tables/APIs kept (hr_assign_shift conflict guard reads them); `hr-roster-view.tsx` remains canonical (isAdmin read-only guard).
  - **Verified**: build green (0 errors; one recharts `Tooltip formatter` typing fix — `(v) => [String(v ?? 0), "staff"]`), prod restarted on :3000, `hr-policy-smoke.mjs` **35/35 green vs prod**: policy defaults GET, PUT 25/75/8 round-trip + synced flag, balances propagated for BOTH a QA staff row and a REAL tenant staff row, staff PUT creates profile (hire_date/grade/base_salary all persisted and read back), restore-to-defaults, zero QA leftovers (staff_profiles → staff → users → auth + audit rows `%leave%`/`%Updated HR profile%` swept). Bundle verified: "Annual leave entitlements", "days / year", "Profile saved", "Paternity", "Emergency" in built chunks. Smoke gotchas: `profiles` embed is object-or-null (never `[0]`); QA users must be created via `SVC.auth.admin.createUser` + users mirror + staff row (known password `QaPassw0rd!123`, sign-in on a SEPARATE AUTH client for the session cookie).
- **HR bulk roster assignment + roster view filters + leave balances card (Aug 15)** — user requested bulk shift/roster assignment viewable by department/individual/role/shift/status "like the removed Roster page", and per-user leave balances visible before applying:
  - **API**: `POST /api/hr/roster/bulk` (HR admin via `isHrAdmin`) — `{ shiftId, from, to, notes?, staffIds[] }`; validates shift exists+active, dates `YYYY-MM-DD` from ≤ to, ≤100 staff, ≤31 days, ≤1000 (staff×days) pairs; loops `hr_assign_shift` per pair; returns `{ assigned, skipped: [{staff_id, date, reason}], errors, total }` (skip reasons: `CREDENTIAL_REQUIRED` for clinical roles without a verified credential, `SHIFT_CONFLICT`/duplicate-key for already-assigned dates — re-runs return 0 assigned/all skipped); audit "Bulk-assigned N shift(s)".
  - **UI** (`hr-roster-view.tsx`): view filter row derived from the month grid — Role / Department / Shift / Status selects + Clear filters (extends `FilterBar` `onClear`); admin **Bulk assign** button + modal: shift picker (name + time + ward), from/to, notes, staff checklist (active staff only) narrowed by department/role selects with "Select all N" / Clear, live counter "X staff × D days = M shifts"; results banner lists skip reasons; reloads grid on success.
  - **Leave balances card** (`leave-view.tsx`): "My leave balances · YYYY" below the header — `GET /api/hr/leave-balances?year=YYYY&staff_id=me`; per-type tiles (remaining big, "X used · Y entitled"), color-coded, "N day(s) remaining" pill; `staff_id` param scopes ANY caller to self (route applies it when truthy); `/api/staff/leave` LEAVE_TYPES now 7 (emergency + paternity added).
  - **POSTGREST TO-ONE EMBED LANDMINE (proved Aug 15, pre-existing)**: filters referencing to-one embedded columns are **silently IGNORED** — `staff?users.role=eq.doctor` returns ALL rows (same result as no filter; count included!), `leave_balances&staff.id=eq.X` returns all rows; and `.or("users.full_name.ilike.%x%")` doesn't no-op but **throws PGRST100 parse errors** ("unexpected f expecting not or operator"). Without the embed in `select` you get PGRST108. Direct FK-column filters work. Fix pattern: filter the direct column (`.eq("staff_id", me.id)`) or resolve ids via the parent table and `.in()` them (HR staff `role` + `q` search now do exactly this; the `q` param previously **400'd on every search** — now searches staff_number/department/specialization + users full_name/email via id resolution).
  - **Verified**: clean build (`Remove-Item .next` first), prod :3000, `hr-bulk-smoke.mjs` **39/39 green vs prod** (bulk 4 assigned / doctor 2 credential-skips / re-run dup skips / 4 guard 400s, emergency+paternity leave 201, balances `staff_id=me` → ONLY own 7 rows, admin w/o param sees all, `role=doctor` → doctors only, `q=edi` finds the doctor, zero leftovers incl. audit). **Chunk map note**: API route code compiles into `.next/server/chunks/[root-of-the-server]__*.js` but client + SSR copies live in `.next/server/chunks/ssr/_*.js` — non-recursive searches miss them; use `-LiteralPath` on names containing `[`.
- **HR Staff Profiles: insight cards + auto-refresh after save (Aug 15)** — follow-up on the HR batch-2 staff page (`hr-staff-view.tsx`):
  - **Six insight cards** above the filters (grid-cols-2 / sm:3 / xl:6), computed client-side from the loaded staff list (no new API): **Staff** (count + active sub), **Department** (distinct count + first 3 names), **Type** (distinct employment types + list), **Base salary** (gross monthly payroll sum), **Hired** (count with recorded hire_date + "N this year"), **Grade** (distinct levels + first 3). Each card: per-color linear-gradient tint + blurred radial glow blob, lucide icon chip (`animate-pop`), count-up animation (`useCountUp` — requestAnimationFrame + cubic ease-out, re-runs when the value changes), staggered entrance (`animate-rise` keyframes + `animationDelay` 60ms steps, hover `-translate-y-1` lift). New keyframes `rise`/`pop` added to `globals.css` (Turbopack inlines CSS into JS chunks — `.next/static/css` does NOT exist; search chunks for `animate-rise`).
  - **Edit profile → Save now refreshes the page**: `load(silent = true)` skips the loading spinner when rows already exist; after a successful PUT the modal reloads (`openDetail`), shows "Profile saved ✓", AND the table + cards update instantly without a page reload.
  - **Verified**: build green, prod :3000, bundle strings confirmed ("gross monthly payroll", "with a recorded date", "no departments yet", "this year", "Hired", "animate-rise" ×2 files). No API/DB changes.
  - **Note (same day)**: the user then asked to REMOVE the cards — reverted: cards grid, `StaffStatCard`/`useCountUp`, `stats` useMemo and the card-only icon imports all deleted from `hr-staff-view.tsx` (auto-refresh-after-save behaviour KEPT — `load(true)` silent reload stays). Verified: build green, :3000, all six card strings absent from all 1365 built chunks.
- **Branch manager + staff branch picker (Aug 14)** — follow-up to branch pricing: hospitals can now manage their branches and assign staff to them explicitly:
  - **API**: `POST /api/pharmacy/admin/branches` (ADMIN roles; name required, case-insensitive duplicate check vs tenant, full contact fields, `is_main` forced false, audits `entityType: "branches"`; returns 201 + RAW snake row) and new `PUT|DELETE /api/pharmacy/admin/branches/[id]` (tenant-scoped, dup-name check excluding self, `isActive` toggle, 404 unknown id; DELETE blocked for the main branch (`ValidationError 400 "The main branch cannot be deleted"`) and — after the DB's FK `SET NULL`s `users.branch_id` — ALSO clears each affected staff member's auth `app_metadata.branch_id` so the next login has no stale claim; audits both). GET now returns the contact fields too and orders main-first. Schema is safe for deletes: `users.branch_id`/`pharmacy_stock_batches.branch_id` SET NULL, `pharmacy_branch_stock.branch_id` CASCADE, `pharmacy_price_overrides` cascades with its drug.
  - **Staff assignment**: `POST /api/admin/users` accepts `branchId` (explicit wins, validated tenant-owned → else 400 "Branch not found in this hospital"; absent → inherits the creator's `ctx.branchId ?? null`; forced null for platform super_admin). `PATCH /api/admin/users/[id]` accepts `branchId` but ONLY when the key is present (explicit `null`/`""` = branchless) — **this kills the old quirk where every PATCH rewrote `app_metadata.branch_id` to the EDITOR's claim**; the app_metadata sync now writes `patch.branch_id ?? user.branch_id` and `tenant_id: user.tenant_id`. GET list embeds `branches!users_branch_id_fkey(name, code)` (singular object — verified live).
  - **UI**: `staff-management.tsx` — Branch select in the Create-account and Edit-details modals ("— No branch (all branches) —" + names with "(main)"/"(inactive)"), branch row (MapPin) on staff cards; branches fetched from the staff-readable GET. The Branches manager renders on **`/app/pharmacy/prices`** via a new `BranchAdminTabs` client shell ("Branch prices" | "Branches" tab toggle, admin-gated page already) — **do NOT mount the old tabbed `PharmacyAdminView` default export: it is legacy dead code never rendered by any route** (the inventory page uses `PharmacyInventoryShell` with only Stock/Import tabs). Branch tab card grid: main badge / active chip / address / phone / email, Add/Edit modal (name required + code/address/city/state/phone/email), Activate/Deactivate, Delete (disabled + title tooltip for main, confirm warns "Staff assigned to it become branchless…").
  - **Verified**: build green, prod restarted on :3000, `branch-manager-smoke.mjs` **39/39 green vs prod** (branch CRUD incl. dup/empty/main-guard/404s/doctor-403, staff assign with branchId + inherit-null + unknown-branch 400 both POST/PATCH, role-only PATCH leaves branch intact — the quirk is dead, explicit null clears users AND auth metadata, branch delete SET NULLs staff + nulls their auth claim, main branch + real branches untouched, zero leftovers) and `branch-price-smoke.mjs` regression still **19/19**. Bundle verified ("Add branch", "No branch (all branches)", "The main branch cannot be deleted", "Branches").
  - **Smoke gotchas (assertion contract)**: branch POST/PUT return the RAW row (`is_main`/`is_active` snake_case) while GET maps camel (`isMain`/`isActive`) — like the admin drugs endpoints; `POST /api/admin/users` returns **200** (`ok()` default), NOT 201; GoTrue **drops null keys from app_metadata** on `updateUserById` (branch_id key absent after a null write — `== null` on the parsed metadata, absent is branchless everywhere).
- **Branch-aware sale pricing wired end-to-end (Aug 14)** — catalogue overrides existed (stock view "Effective" column), but sales priced from the fixed catalogue `unit_price` at sale time. Now:
  - `frontend/src/lib/pharmacy-pricing.ts` — shared `resolveEffectivePrices(svc, tenantId, branchId, drugIds)` mirroring SQL `effective_drug_price` (exact-branch override → branch-NULL override → unit_price → wholesale), returns `{ price, source }` per drug.
  - `GET /api/pharmacy/drugs`: branch overrides COLLIDED in the response map (multiple branch rows per drug under one key — net price was garbage); now filtered by `branch_id` param ?? `ctx.branchId`; response adds `priceSource` (`branch_override` | `base_override` | `catalog` | `wholesale`).
  - `GET /api/pharmacy/inventory`: `effectivePrice`/`priceSource` now branch-aware (uuid `branch` param beats the branch claim, else claim, else base-only).
  - `POST /api/pharmacy/invoices`: `p_branch_id` defaults to `body.branchId ?? ctx.branchId` (SQL resolution already branch-aware via `effective_drug_price`).
  - `POST /api/prescriptions/[id]/convert-sale` + `createPrescriptionInvoice` (auto-invoice on full dispense): pricing branch = `ctx.branchId ?? rx.branch_id`.
  - `NewSaleModal` (pharmacy-billing-view.tsx): amber "Branch price" / sky "All-branch price" chips on picker rows + invoice line rows (driven by `priceSource`).
  - Cleanup migration `0080_qa_branchprice_fixture_purge.sql` APPLIED (first smoke run polluted append-only `dispensing_audit_logs`; drop/delete/recreate `trg_dal_immutable` + `trg_pharmacy_branch_stock` — 0071–0073 pattern).
  - Verified: `temp\opencode\branch-price-smoke.mjs` — **19/19 green, zero leftovers** (catalog + inventory + invoices legs; NO convert-sale/dispense leg by design — see Gotchas). Bundle verified ("Branch price"/"All-branch price" in built chunk).
- **Partial receive form: only pending lines shown (Aug 14)** — `ReceiveGoodsModal` (`vendor-purchasing.tsx`, PurchaseOrdersTab) previously rendered EVERY PO line in the Receive goods form, fully-received ones included ("Line · up to 0 remaining"), so partial receipts meant scrolling past dozens of done lines (user report: PO-2026-004 with 13 lines, 12 already received). Fix: `lines` initial state now filters to `quantityOrdered - quantityReceived > 0`; the modal shows an amber summary "N of M line(s) already received — showing only the X line(s) still pending", an "All lines on this purchase order have been fully received." empty state (submit disabled) when nothing remains, and the submit button disables when `lines.length === 0`. Pure client-side — the `pharmacy_grn_receive` RPC already only processes the lines the payload sends. Build green, :3000 restarted, bundle strings verified ("still pending", "All lines on this purchase order").
- **Drug CSV import: replace-vs-keep conflict flow (Aug 14)** — the Import tab no longer blows up with `bulk insert failed: duplicate key value violates unique constraint "uq_pharmacy_drug"` when a large file re-imports existing catalogue names. **Root cause**: the route's existing-name lookup was ONE `in("name_normalized", names)` query — with hundreds of names the PostgREST URL overflows, the lookup fails silently, `byNorm` stays empty, every row is treated as new and the bulk insert trips `uq_pharmacy_drug` (0023, `UNIQUE (tenant_id, name)`). **Fix**: lookup chunked in batches of 100 (failure now throws instead of being ignored).
  - **Backend** (`POST /api/pharmacy/admin/import`): new `dryRun: true` → validate + match, return `{ total, existing, errors }` WITHOUT writing (200); new `conflictAction: "replace" | "keep"` — replace = existing rows updated in place with CSV values (legacy default), keep = existing rows untouched (counted in `skipped`, no error rows); `skipExisting: true` kept as an alias for keep; response now also carries `existing` (count of rows already in catalogue). dryRun does NOT resolve per-row supplier errors (that happens in the write loop) — the client prompts on `existing > 0`, then the real run reports supplier/validation issues.
  - **UI** (ImportTab in `pharmacy-admin-view.tsx`): the "Skip rows that already exist" checkbox is GONE; Import now always dry-runs first, and when `existing > 0` shows a confirm modal "Existing drugs found — N of the drug(s) in this file already exist in the catalogue. Replace them … or skip them and only add the new drugs?" with **Replace them** (primary → conflictAction replace) / **Keep existing (add new only)** (→ keep) / Cancel; no existing → straight create.
  - **Verified**: build green, prod restarted on :3000, `import-conflict-smoke.mjs` 16/16 vs prod: dryRun all-new 0 / mixed 3, keep → created 0 + skipped 3 + untouched prices, replace → updated 3 + prices replaced + supplier linked, unknown-supplier row rejected as per-row error in BOTH modes, legacy skipExisting alias, **big-file leg: 250 real tenant drugs + 25 new → updated 250 / created 25 / ZERO errors** (chunked lookup proven), real drugs intact after, zero leftovers (28 fixture drugs + supplier + QA user+auth removed). Bundle verified: "Existing drugs found", "Keep existing (add new only)". Smoke lesson: the unknown-supplier row is a row ERROR (never created) regardless of conflictAction — don't assert it into `created`.
- **Drug catalogue CSV import: supplier column (Aug 13)** — follow-up to the drug→supplier linkage (migration 0078): the admin Import tab (`/app/pharmacy/inventory`) can now wire imported drugs straight to a tenant supplier by name:
  - **`POST /api/pharmacy/admin/import`** accepts optional `supplier`/`supplier_name` CSV header (alias map extended: also `vendors`, `vendor`, `vendor_name`, `vendor name`, `suppliers`, `suppliers name`, `supplier name`, `manufacturer`, `distributor` — the app's own stock-view CSV export names the column **`suppliers`**, which was the cause of an Aug 14 bug report). Names matched case-insensitively (`toLowerCase` + `trim` on BOTH sides, exact, no fuzzy) against the tenant's `pharmacy_suppliers`; known supplier → `supplier_id` set on BOTH create and update paths; unknown → per-row error `unknown supplier "<name>" — add it on the Suppliers tab first`, that row skipped entirely (drug not created/not updated); blank cell → column omitted (existing links untouched on re-import). One tenant lookup, no extra migrations (0078 column).
  - **Inventory API** (`GET /api/pharmacy/inventory`, used by the stock view and the PO grid) already joined `pharmacy_suppliers(name)` → `supplierId`/`supplierName`.
  - **UI**: stock-view table gained a **Supplier** column (name or `—`; colSpans 7→8, interface + row cell + header); `ImportTab` TEMPLATE gained the `supplier` column (both sample rows reference real Nigerian suppliers) + description note "supplier (optional — the supplier's exact name; it must already exist on the Suppliers tab)"; `CreateOrderModal` (vendor-purchasing.tsx) "Only <supplier>'s drugs" toggle with zero linked drugs now shows an actionable empty state ("link drugs to it in Pharmacy → Inventory (the Supplier column) or in the drug CSV import … turn this toggle off to pick from the full catalogue") instead of the generic "No drugs match this search."
  - **Verified**: build green (0 errors), prod restarted on :3000; `import-supplier-smoke.mjs` 16/16 vs prod on the live tenant: fresh import created 2 (A linked to supplier A, unknown-name row → error + drug NOT created, B null), inventory API returns supplierId + supplierName, re-import swaps A→supplier B (updated 1), unknown supplier on existing drug → error row + drug untouched, blank-cell re-import keeps the link, zero leftovers (drugs + suppliers + QA users+auth removed). Bundle verified: "No drugs are linked to", `nafdac_number,supplier` template, "it must already exist on the Suppliers tab". **Follow-up (Aug 14)**: users' CSVs with the header spelled "Supplier Name" (or vendor variants) were silently ignored — alias map extended (also `vendor`, `vendor_name`, `vendor name`, `manufacturer`, `distributor`) and the map key + match both `trim()` (a DB supplier name with a trailing space previously failed exact match). `supplier-header-variants.mjs` 6/6: "Supplier Name" header, mixed-case/trailing-space suppliers link correctly, blank stays null, unknown still error-rows.
- **Supplier opening-balances CSV import (Aug 13)** — the Balances tab (`/app/pharmacy/suppliers`) previously showed all-₦0 rows for migrating hospitals and its Import menu was a hard block ("balances are calculated from POs/payments"). Now tenants can seed carried-forward balances from their old system/spreadsheet WITHOUT faking POs/GRNs/payments:
  - **Migration `0079_supplier_opening_balances.sql` (APPLIED via CLI)** — `pharmacy_supplier_opening_balances`: one row per `(tenant_id, supplier_id)` UNIQUE, `total_bought`/`total_paid numeric(14,2) CHECK >= 0`, notes, created_by → users, `ON DELETE CASCADE` from `pharmacy_suppliers`, RLS staff-read policy mirroring `supplier_payments` (writes via service client, audited), `GRANT SELECT` to authenticated.
  - **`POST /api/pharmacy/procurement/balances/import`** (PROCUREMENT_TEAM): `{ rows: [{supplierName, totalBought, totalPaid, notes?}] }` — money cells parsed by stripping `₦`/commas (empty → 0), negatives → per-row error, names matched case-insensitively (`ilike` exact) against the tenant's suppliers, unknown → error row "add it on the Suppliers tab first"; known suppliers UPSERTed on `(tenant_id, supplier_id)` (created/updated counted via a pre-fetch, re-import overwrites); one audit entry `entityId: bulk/<created>/<updated>`; returns `{ imported, created, updated, errors }` 200 (lab-import style, NOT the PO import's 400-on-all-invalid).
  - **`GET /api/pharmacy/procurement/summary`** now folds opening rows into every supplier's `totalBought`/`totalPaid` (outstanding recomputed as bought − paid so negatives show "(credit)") and into the totals cards, and exposes `openingBought`/`openingPaid` per row. `totalOrdered`/PO/GRN/payment counts untouched (opening is not activity).
  - **UI** (BalancesTab): blocked import replaced with a live one — columns `supplier_name, total_bought, total_paid, notes` + sample + description explaining migration use + overwrite semantics; result note "Opening balance saved for N supplier(s)"; rows with an opening show an amber "+₦X opening balance" chip under the supplier name; CSV/PDF exports gained an "Opening bought" column.
  - **Verified**: build green (0 errors), prod restarted on :3000, `opening-balance-smoke.cjs` 20/20 vs prod: baseline zero → import "3,500,000" (comma stripping) w/ notes + unknown-name error row → summary folds bought/paid/outstanding 3.5m/2m/1.5m + opening fields + creator stored → re-import updates in place (created 0/updated 1, values change) → negative rejected as row error → empty rows 400 → totals cards include opening → cleanup cascades opening row with the supplier → zero leftovers. Bundle strings verified: "Import opening balances", "supplier-opening-balances-template.csv", "opening balance", "Opening balance saved".
- **Drug → supplier linkage + bulk PO grid + PO line import (Aug 13)** — follow-up to the empty drug-dropdown fix:
  - **Migration `0078_pharmacy_drugs_supplier.sql` (APPLIED via CLI)** — `pharmacy_drugs.supplier_id uuid → pharmacy_suppliers(id) ON DELETE SET NULL` + partial index. Verified live (column present; deleting a supplier keeps the drug).
  - **Supplier field on drugs**: `DrugInput`/`validateDrugInput`/`drugUpdateColumns` (`lib/pharmacy-admin.ts`) accept `supplierId`; POST + [id] PATCH (both full-doc and partial paths) validate it against the tenant's suppliers (400 "Supplier not found in this hospital"); GET `api/pharmacy/drugs` + admin drugs map it as `supplierId` (`search_pharmacy_drugs` returns `SELECT *` so it flows through). `DrugFormModal` (Drugs tab + stock-view edit) gained a "Primary supplier" select (`— None —` + active suppliers), pre-filled on edit.
  - **CreateOrderModal → bulk grid** (`vendor-purchasing.tsx`): the old "Add line" select-per-row is gone. Now a filtered, quantity/cost-entry GRID over the whole catalog: search box, "Only <supplier>’s drugs" toggle (default off — never an empty list again), supplier-tagged drugs PIN to the top (★ keeps marking preferred offers), offers still pre-fill unit cost into the grid on supplier pick, "chosen" count + live total. Empty-catalog hint stays.
  - **Bulk PO import** — new `POST /api/pharmacy/procurement/purchase-orders/import` route (PROCUREMENT_TEAM roles; mirror of the POST PO route): `{ supplierId, rows: [{drugName, quantity, unitCost, notes?}] }`; name matched case-insensitively against the tenant catalog (`ilike` exact), unknown names → error rows (1-indexed incl. header), valid lines → ONE draft PO via `pharmacy_po_create`; all-invalid → 400 with the first error; audit `bulk/`-style entry; returns `{ id, supplierName, rowsCreated, errors }` 201. PurchaseOrdersTab's previously-blocked Import menu is live: supplier picker rendered INSIDE `CsvImportModal` via new `extraContent` prop (threaded through `ImportExportMenu` as `importExtra`), columns `drug_name, quantity, unit_cost` + sample, result notes "PO created for <supplier>".
  - **Verified**: build green (0 errors); `supplier-link-smoke.mjs` 24/24 vs prod on the live tenant: catalog `supplierId` field, drug create/PATCH (null-clear + re-set + unknown-supplier 400 on both POST and PATCH), bulk import (1 valid + 1 unknown → created with correct PO total + error row), missing-supplier/empty-rows/all-invalid 400s, `ON DELETE SET NULL` (PO must be deleted BEFORE the supplier — `pharmacy_purchase_orders_supplier_id_fkey` is plain RESTRICT), zero leftovers. Smoke gotcha: admin drug POST/PATCH return the RAW row (snake `supplier_id`), while the catalog/`api/pharmacy/drugs` maps camel — assert accordingly. Bundle verified: "Primary supplier", "Tagged to this supplier", "Import purchase order lines", `purchase-orders/import` present in deployed chunks; prod restarted on :3000.
- **New Purchase Order drug dropdown empty (Aug 13)** — the "Drug" select in `CreateOrderModal` (`vendor-purchasing.tsx`) populated ONLY from `GET /api/pharmacy/procurement/supplier-offers?supplier_id=` (`supplier_drug_prices` rows). The tenant has 362 catalog drugs but ZERO pricing offers → the dropdown rendered just "Select drug…". Fix: the modal now loads the full catalog via `GET /api/pharmacy/drugs` on mount (`drugOptions` memo merges offer-only drugs in, alphabetical, `(form)` suffix, ★ when the supplier marks the drug preferred); offers still pre-fill unit cost in `pickDrug`; added "No drugs in the catalog yet" empty hint. Note the "enter unit costs manually" hint stays — no offers just means manual costs, never an empty list. Build green, :3000 restarted, bundle verified ("No drugs in the catalog yet" present), catalog RPC verified live returning 362 rows w/ name+form.
- **Lab services re-categorised + price-category guard (Aug 13)** — the user reported the Lab → Services tab "groups services by price": the tab WAS grouping by category, but the tenant's categories were literally price figures (`2000`…`75000`, 9 groups, all 49 services) — a bad CSV import had put the price column into the category slot. Fixed data + hardened the system: (1) **data re-categorisation** (`C:\Users\Admin\AppData\Local\Temp\opencode\lab-recat.cjs`): all 49 services re-pointed into 5 real groups — Haematology (9), Chemical Pathology (9), Microbiology & Serology (13), Histopathology (5), Imaging & Cardiology (13) — 9 price-named categories deleted, one audit row (`role: service_fix`, `entityId: recat/49`), zero unassigned; (2) **guards**: `POST /api/lab-services`, the import route and the AddServiceModal now reject numeric-only category names (`/^\d+(\.\d+)?$/` → import: error row `Category "<n>" looks like a price — ignored; use a real category name` and the service still imports without a category; POST: 400; modal: inline error, nothing submitted). Edit modal is select-only (no free text). Verified live 7/7 (`lab-category-guard-smoke.mjs`): numeric import → created 1 + warning + no category + no `5000` row created, POST 400, real category still creates 201, cleanup zero leftovers. Build green, :3000 restarted, guard string in bundle.
- **Lab Services page: row Actions + bulk delete + CSV import upsert (Aug 13)** — the `/app/lab` Services tab (`components/dashboard/lab-view.tsx`) now has: (1) a per-row **Actions dropdown** (ActionDropdown) with View (new read-only `ViewServiceModal`: type, price, group, availability, reference range, external lab ID, approval + custom chips), Edit (existing modal) and Delete (now available for ALL services to admins, not just `is_custom`); (2) **bulk delete** for admins (`canManageCatalog`): per-row checkboxes + "Select all / Deselect all" toggle + "Delete selected (n)" button in the filter bar (red), which confirms with the service names then loops `DELETE /api/lab-services/[id]` and reports a count of failures; (3) **CSV import now UPSERTs** — new `POST /api/lab-services/import` route (`src/app/api/lab-services/import/route.ts`): tenant + case-insensitive exact-name match; existing name → PATCH in place (price/type/category/ref-range/external id) when caller is hospital_admin/super_admin/lab_tech, else an error row `"<name>" already exists (only hospital admins and lab staff can update it)`; new name → create with `is_custom: true` (approved immediately for admins, pending otherwise); `newCategory` gets-or-creates the `lab_categories` row (admin only, else falls back to no category); returns `ok({ created, updated, errors: [{row, message}] })` and one audit entry (`entityId: bulk/<created>/<updated>`). 1-indexed `row` (2 = first data row). `importServices` client-side now calls the one route and shows "X existing service(s) updated" / "X new service(s) added" notes — the duplicate-name path that previously killed whole imports ("already exists") is gone. Delete stays admins-only 400 ValidationError per the `[id]` route (doctor → 400 "Only hospital admins can delete services"). No migration (lab_request_items.service_id is ON DELETE SET NULL — deletion is safe).
  - **Verified**: `npm run build` green; restarted :3000; bundle contains "Deselect all"/"Delete selected" + `lab-services/import`. Smoke `C:\Users\Admin\AppData\Local\Temp\opencode\lab-service-import-smoke.mjs` 17/17 vs prod: created 2 (price/type/category/meta correct, category auto-created + linked), updated 1 in place (live "Abdominal Ultrasound" price bumped then restored to 1975 after the run), re-import → created 0/updated 3 + prices actually changed, doctor import creates new (pending) + error row on existing name, doctor DELETE blocked + row survives, admin DELETE removes. **Smoke gotchas**: import returns 200 not 201; `rowsA?.every()` on an empty array vacuous-passes — always length-guard; ilike pattern must be `QA Import %${ts}` (ts is mid-name); the FIRST run left QA fixtures + a +3950 price bump on Abdominal Ultrasound because of the bad pattern and a failed category FK delete — swept after (restore = final − 2×1975 = 1975) with run-1 QA auth accounts removed too; final state zero leftovers.
- **Tenant purge — 36 tenants → ZERO (Aug 12)** — removed **36 of 36 registered tenants** in 3 rounds with ALL their accounts (staff + patients, auth + users mirror) and data, per user request: round 1 kept Liamsfield Hospitals (`fe46a3bb`, 20 users) + demoCare (`10000000-…-0001`, 7 users) + LiamsField (`1b4741cf`, 0 users); round 2 removed fe46a3bb; round 3 removed demoCare + LiamsField ("clean for fresh signups"). Deleted: all `E2E Analytics/Lab/Ward/HR *`, `RxMail Probe *`, `Isolation Test Clinic`, Liamsfield Hospitals, demoCare, LiamsField. Leftover sweep also deleted orphan auth `pdfe2e.1786144219861@qa.local` (no users row, no tenant).
  - Method: service-client REST wipe per tenant — ~80 tenant_id-scoped tables (`?tenant_id=eq.` DELETE, multi-pass retry on FK), then `users` mirror rows, then `POST /auth/v1/admin/users/{id}` DELETE per auth account, then avatars-storage prefixes (`tenants/<tid>/`, `users/<uid>/`), then the `tenants` row. **Gotcha: several tables have NO `tenant_id` and must be removed via their tenant-scoped parents' ON DELETE CASCADE** — `beds→wards`, `drug_batches→drugs`, `invoice_items→invoices`, `chat_messages→chats`, `internal_message_recipients→internal_messages`, `lab_order_tests/lab_request_items/lab_results→lab_orders/lab_requests`, `pharmacy_branch_stock`, `po_items`, `prescription_items`, `pharmacy_grn_items`, `pharmacy_invoice_items`, `pharmacy_purchase_order_items`, `push_subscriptions` (FK→users, cascades from the users-mirror delete).
  - **Append-only tables**: `controlled_drug_register` (trg_cdr_immutable), `dispensing_audit_logs` (trg_dal_immutable) AND `pharmacy_ai_decisions` (trg_ai_decisions_immutable, migration 0043) RAISE on DELETE even for service role. Migrations **`0071`** (round 1) and **`0072`** (round 2) drop the cdr/dal triggers, delete rows `WHERE tenant_id NOT IN (kept ids)`, recreate them; **`0073_purge_liamsfield_pharmacy.sql`** handles the pharmacy blocker: `pharmacy_stock_batches`' AFTER-DELETE trigger `trg_pharmacy_branch_stock` (migration 0033) re-INSERTs a `pharmacy_branch_stock` row per deleted batch — once the parent `pharmacy_drugs` row is cascade-deleted that INSERT 23503s, which also froze the API wipe on `pharmacy_drugs`/`pharmacy_stock_batches` (endless 23503 retry). 0073 drops trg_ai_decisions_immutable + trg_pharmacy_branch_stock, deletes ai_decisions → stock_movements → stock_batches → branch_stock (via drug/branch ids) → drugs, recreates both triggers. Also: `notifications` bulk DELETE can 400 transiently inside the multi-table pass but works standalone.
  - **Verified final state**: ZERO tenants total; every tenant-scoped table EMPTY (verified table-by-table); only remaining rows = platform account `platform@skycare.app` (super_admin, tenant NULL: users row + 1 audit + 1 security row = legit global rows), global pharmacy reference seed (4 tables, tenant NULL), empty `avatars/users/` folder; auth exactly 1 account (platform@). Wipe scripts: `C:\Users\Admin\AppData\Local\Temp\opencode\tenant-wipe.cjs` / `tenant-wipe2.cjs` (KEEP array; round 3 = empty) / `verify-zero.cjs` (global zero-check, chat_presence uses `select=*` — see gotcha).
- **Tenant onboarding reworked (Aug 12)** — prospective tenants no longer type a slug; the signup form (`/signup`) now has an optional **Website** field (their own external link) and the `<slug>.skycare.app` subdomain is **auto-assigned after signup**, generated from the hospital name (lowercase, `[^a-z0-9]+`→`-`, ≤40 chars, fallback `hospital-<ts>`; collisions get `-2`, `-3`…). Migration **`0074_add_tenant_website_url.sql`** (APPLIED) adds `tenants.website_url text` for the external link (the existing `tenants.website jsonb` stays the in-app website-generator content). `tenant-onboarding` edge function REDEPLOYED: accepts `slug` only for internal/tests (still validated `^[a-z0-9-]+$`), validates `website` (`^https?://`, ≤200 chars → else 400 with clear message), stores `website_url`, returns `subdomain` with the final slug. The tenant landing page `[slug]/page.tsx` Contact grid renders a clickable **Website** card when `website_url` is set. Probed: bad URL → 400; no-slug registration → 201 + auto slug; duplicate name → `-2` suffix; landing renders link (round-trip on the live tenant, reverted). Signup SSR verified (helper text "Your free site … created automatically after signup"). **Same-day follow-up (dup-email fix)**: the function originally inserted the tenant BEFORE creating the auth user, so a "A user with this email address has already been registered" failure left an orphan tenant per attempt (observed: one orphan `sluggino`-style row per failed retry). Rewritten + REDEPLOYED: auth user created FIRST (duplicate emails fail with nothing written), then tenant/branch/users-mirror/app_metadata/staff, each failure rolling back everything created so far (`svc.auth.admin.deleteUser` + tenant delete). Probed: dup email → 409 + tenants count unchanged; fresh email → 201 + auto slug + website_url. Note: the user's live registration (LiamsField Hospital, slug `liamsfields` = `b9290c07-456f-4951-ae86-37a64dacc486`, admin olalekan.edun@gmail.com) predates this (website_url NULL).
  - Method: service-client REST wipe per tenant — ~80 tenant_id-scoped tables (`?tenant_id=eq.` DELETE, multi-pass retry on FK), then `users` mirror rows, then `POST /auth/v1/admin/users/{id}` DELETE per auth account, then avatars-storage prefixes (`tenants/<tid>/`, `users/<uid>/`), then the `tenants` row. **Gotcha: several tables have NO `tenant_id` and must be removed via their tenant-scoped parents' ON DELETE CASCADE** — `beds→wards`, `drug_batches→drugs`, `invoice_items→invoices`, `chat_messages→chats`, `internal_message_recipients→internal_messages`, `lab_order_tests/lab_request_items/lab_results→lab_orders/lab_requests`, `pharmacy_branch_stock`, `po_items`, `prescription_items`, `pharmacy_grn_items`, `pharmacy_invoice_items`, `pharmacy_purchase_order_items`, `push_subscriptions` (FK→users, cascades from the users-mirror delete) — deleting the tenant_id parents cascades them (proved: tenant-row DELETE succeeded only where this held).
  - **Append-only tables**: `controlled_drug_register` (trg_cdr_immutable), `dispensing_audit_logs` (trg_dal_immutable) AND `pharmacy_ai_decisions` (trg_ai_decisions_immutable, migration 0043) RAISE on DELETE even for service role. Migrations **`0071`** (round 1) and **`0072`** (round 2: fe46a3bb) drop the cdr/dal triggers, delete rows `WHERE tenant_id NOT IN (kept ids)`, recreate them; **`0073_purge_liamsfield_pharmacy.sql`** handles the pharmacy blocker: `pharmacy_stock_batches`' AFTER-DELETE trigger `trg_pharmacy_branch_stock` (migration 0033) re-INSERTs a `pharmacy_branch_stock` row per deleted batch — once the parent `pharmacy_drugs` row is cascade-deleted that INSERT 23503s (`drug_id not present in pharmacy_drugs`), which also froze the API wipe on `pharmacy_drugs`/`pharmacy_stock_batches` (endless 23503 retry). 0073 drops trg_ai_decisions_immutable + trg_pharmacy_branch_stock, deletes ai_decisions → stock_movements → stock_batches → branch_stock (via drug/branch ids) → drugs, recreates both triggers. Also that round: `notifications` bulk DELETE 400'd transiently inside the multi-table pass but worked standalone — rerun the pass or delete leftovers individually.
  - **Verified final state**: ZERO tenants total (rounds 1+2+3); every tenant-scoped table is EMPTY (verified table-by-table, incl. cascade-only tables); the ONLY remaining rows are the platform account `platform@skycare.app` (super_admin, tenant NULL: users row + 1 audit login + 1 security failed_login, both tenant NULL = legit global rows), the global pharmacy reference seed (4 tables, tenant NULL), and the empty `avatars/users/` folder; auth has exactly 1 account (platform@); logins for `olalekan.edun@gmail.com`, `pttrans.1786461970067@qa.local`, `iso.123754@skycare.app`, `smoketest@skycare.app` all → 400 (no auth accounts). Wipe scripts: `C:\Users\Admin\AppData\Local\Temp\opencode\tenant-wipe.cjs` / `tenant-wipe2.cjs` (KEEP = kept ids; round 3 = empty KEEP) / `verify-zero.cjs` (global zero-check, chat_presence uses `select=*` — see gotcha).
- **Search + From/To date filters on 4 pages + Ward-transfer dropdown fix (Aug 12)** — built, deployed to :3000 and smoke-verified 17/17 (`C:\Users\Admin\AppData\Local\Temp\opencode\filters-smoke.mjs`; used a temporary QA hospital_admin on the real tenant `fe46a3bb-44e0-4e4b-a83d-ff190d1d3ea3` since `smoketest@skycare.app`'s AUTH ACCOUNT NO LONGER EXISTS — only its users-table row did; the demoCare tenant `10000000-…-0001` has auth for nobody; QA fixtures like `pttrans.*@qa.local`/`pdfe2e.*@qa.local` lived on `fe46a3bb` — **GONE since the round-2 purge** (see purge entry above):
  - **Pharmacy Compliance** (`/app/pharmacy/compliance`): every tab now has a `FilterBar` search + From/To (`src/components/dashboard/pharmacy-compliance-view.tsx`): Alerts (search type/drug/severity/message, date on created_at), Controlled register (drug/patient/prescriber + date), Audit trail (drug/action/actor/hash + date, plus the hash-chain verify banner), Dispense (search filters the controlled formulary + added "Recent controlled dispensing" history table fetched from `/api/pharmacy/compliance/register?pageSize=200`, date filters the history), NAFDAC reports (search over report rows; tab already has its own From/To window picks). All `inDateRange` client-side; exports respect the visible (filtered) rows.
  - **Bed Map** (`/app/wards/bed-map`): FilterBar (ward/bed-no/patient search) + status chips All/Available/Occupied/Maintenance/Cleaning + From/To. **Change made this session**: the date range no longer hides vacant beds — it only constrains *occupied* beds to admissions admitted within the period (helper text updated); previously any date range blanked every bed without an occupant.
  - **HR Dashboard** (`/app/hr`): FilterBar with search (alerts/departments/leave) and From/To passed to `GET /api/hr/dashboard?from&to` (route already extended; `hr_dashboard` RPC gained `p_from`/`p_to` in migration **0069** — applied to prod, verified responding). "Month rate" card/leave label flip to "Period" when a range is set.
  - **Shifts & Roster** (`/app/hr/roster`): FilterBar (staff/role/shift/status) + From/To applied client-side over the month grid (filters the `byDate` map + exports + assignment count).
  - **Ward-transfer dropdown fix**: root cause = the deployed build's Transfer button never ran `loadLookups()`, so `/api/wards` was never fetched for the modal unless "Admit patient" had been opened in-session — `wards` stayed `[]` and "Select bed…" had zero options. Working tree already added `void loadLookups()` to the transfer button; this session also **excludes the patient's current bed** from the destination list (uses `transferFor.beds.id` with array-guard) and shows an amber "No other beds are currently available to transfer into." note. Live-verified: `GET /api/wards` returns beds incl. `available` ones for `fe46a3bb` (e.g. FP-001…FP-005, FA-001…FA-007) so the dropdown populates.
  - **Migration `0070_auth_users_fk_fix.sql` (APPLIED)** — **root-cause fixes for permanently broken lists**: `controlled_drug_register.pharmacist_id`, `dispensing_audit_logs.user_id`, `pharmacy_compliance_alerts.resolved_by` and `supplier_payments.created_by` FK'd **`auth.users`** while every API embeds `users(full_name)` against the **`public.users`** mirror ⇒ PostgREST "Could not find a relationship … in the schema cache" ⇒ **Controlled register, Audit trail and supplier-payments GETs 400'd (empty tabs)**. Migration re-points all four FKs to `public.users(id)` (ids mirror auth ids; ON DELETE SET NULL). Verified live after push: register 200 w/ rows + `users(full_name)`, logs 200 incl. `verify=true` chain check (535 rows, unbroken), alerts 200, `/api/pharmacy/procurement/supplier-payments` 200 (that one also powers the Payments tab list in Suppliers & Procurement). `vendor-smoke` had never caught it because it POSTs payments but never GETs the list.
  - Gotcha recorded: **`GET /auth/v1/admin/users` ignores `filter[email]=eq.…`** (returns everything) — never use it to target one user; page through and match client-side, else you'll reset the wrong account (happened with `pttrans.*@qa.local`; fixed to `Patient123!`).
  - Build: `npm run build` green (deleted stale `.next` first), prod restarted on :3000.
- **Staff registration form password reveal (Aug 12)** — the `/app/staff` Create account modal "Login password" input now has an eye toggle (Eye/EyeOff lucide icons, `showStaffPassword` state) so admins can see the password they typed for a new staff member; resets to hidden when the modal closes. Built + restarted on :3000; toggle confirmed in bundle (search `showStaffPassword`).
- **Staff edit form: full-user info editable (Aug 12)** — the `/app/staff` Edit details modal now also corrects **Full name, Email, Phone, Role** (previously only staff-row fields). `PATCH /api/admin/users/[id]` extended to accept `full_name`/`email`/`phone`: email validated + pushed to the auth account FIRST (`email_confirm: true`; duplicates rejected with GoTrue's message before the mirror is touched), then users mirror + app_metadata sync; audit logs the changes. Frontend: new fields in `staff-management.tsx` edit modal (defaults from the user row, change-diff only — unchanged fields aren't sent; `Array.from(new Set([currentRole, ...rolesFor]))` so the current role always stays selectable, e.g. super_admin editing). Build incident same day: `next build` started failing/hanging at Turbopack compile ("Cannot access offset outside the bounds of TypedArrays", then deadlock at 0% CPU with orphaned node.exe processes surviving tool timeouts) — fixed with `pnpm install --force` (refreshed corrupted native `@next/swc` bindings) + fresh `.next`; build green again (~18s). A TS2339 on `form.get("email")` (FormDataEntryValue can be File) was fixed by wrapping in `String(...)`.
- **Banking module: transfers + statements + reconciliation + payroll posting (Aug 13)** — built on migration 0062's read/post APIs (`banking-sources.ts`, `postBankLedger`, `bankLedgerAccountForMethod`):
  - **Migration `0075_banking_extensions.sql` (APPLIED via CLI)** — extends `hospital_bank_ledger.source` CHECK with `transfer`, `opening`, `payroll` (idempotent drop/re-add); adds `transfer_id uuid` (groups a transfer's paired out/in rows; index `idx_hospital_bank_ledger_transfer`) and `payroll_id uuid → payroll_records` (idempotency guard for payroll posting; index).
  - **Internal transfers** — `POST /api/banking/transfers` (admin/cashier/accountant): `{ fromAccount, toAccount, amount, method?, notes?, recordedAt? }` — `"cash"` or bank uuid; validates both accounts active + from≠to; writes TWO ledger rows (out on source with `source_ref: "Transfer to <dest>"`, in on dest "Transfer from <src>") sharing `transfer_id` + `TRF-XXXXXXXX` reference; one audit entry. `DELETE /api/banking/entries/[id]` now also deletes `transfer` rows (both pair sides via transfer_id, deduped set) and `opening` rows.
  - **Opening balances** — `POST /api/banking/entries` accepts `source: "opening"` (direction must be 'in'; one per account — second one 400s; deletable). Opening rows count toward balances but are EXCLUDED from month/period in/out totals (`GET /api/banking` apply() + totals query, statement totals) — carried-forward money, not period activity.
  - **Per-account statement** — `GET /api/banking/statement?account=cash|<uuid>&from=YYYY-MM-DD&to=YYYY-MM-DD` → `{ opening (ledger sum BEFORE from), in, out (both excluding 'opening'), closing, rows: [{…, running_balance}] }` (newest-first, payment rows attributed via `classifyLedgerSources`, `to` normalized to end-of-day). No from/to = full history, opening 0. **Gotcha fixed in dev**: `(r.account_id as string|null) ?? null === accountId` parses as `??  (null === accountId)` (?? binds looser than ===) — bank rows leaked into the Cash statement; fixed with `(r.account_id ?? null) === accountId`. Smoke caught: cash statement closing 150000 instead of 75000.
  - **Payroll auto-posting** — `PUT /api/hr/payroll/[id]` when status → `paid` posts a ledger 'out' (source `payroll`, `payroll_id`, `PR-XXXXXXXX`, default bank via `resolveBankAccountId` else cash, method bank_transfer|cash); guarded by a `hospital_bank_ledger` lookup on `payroll_id` so re-saving 'paid' never double-posts.
  - **UI** (`banking-view.tsx`): Transfer button + modal (From/To/Amount/Method/Date/Notes); every account card has **Statement** (month picker → opening/receipts/payments/closing cards, **Daily summary** per-day in/out/closing table, entries list with running balance, Reconciliation panel: enter the bank's statement closing → balanced/variance message, CSV + letterhead Print via window.open doc.write mirroring financial-reports-view printPnl) and **Opening balance** buttons; transaction rows show "Delete transfer" / "Delete" for adjustment/transfer/opening; `SOURCE_LABELS` gained transfer/opening/payroll.
  - **Smoke**: `C:\Users\Admin\AppData\Local\Temp\opencode\banking-smoke.mjs` — 31/31 vs prod on the live tenant: bank CRUD, openings (dup 400, out 400), transfer + same-account 400, overview balances (cash 75000 = 100k opening − 25k sweep; bank 75000 = 50k opening + 25k in), opening excluded from month totals, statements (cash opening 0/in 0/closing 75000; bank in 25000), unknown account 400, transfer pair delete (deleted: 2), full cleanup, patient 403 on transfers+statement. Payroll source validated end-to-end (insert → read → delete). **Cleanup after the run is CRITICAL**: `SVC.from("users").delete()` can fail silently while `auth.admin.deleteUser` succeeds — final cleanup now logs failures (run#1 left 6 QA users on the live tenant; all purged, incl. audit rows for the QA ledger actions; the user's own 3 real bank accounts Zenith/Access/WEMA — created 22:16 Aug 12 — were untouched and remain). Session cookie for API smokes: `sb-<ref>-auth-token` = `base64-<base64url(JSON session)>` (withAuth reads cookies, NOT Bearer).
- **Expenses "Pay from account" dropdown (Aug 13)** — the Expenses form (Add + Edit, `finance-view.tsx`) now lets the user pick which bank (or Cash) the expense is paid from; the ledger posting debits the chosen account. Migration **`0076_expenses_account_id.sql`** (APPLIED): `expenses.account_id uuid → hospital_bank_accounts` (ON DELETE SET NULL) + index; backfill copies the old behaviour (non-cash rows → tenant's first active bank, cash rows → NULL). New shared resolver `resolvePayingAccountId(svc, tenantId, accountId, fallbackMethod)` in `lib/api-utils.ts`: explicit `"cash"` → null; uuid → validated active tenant bank (else 400); absent/blank → method-derived default (first active bank for non-cash). `POST /api/expenses` accepts camel `accountId`, stores `account_id` + posts ledger with it; `PUT /api/expenses/[id]` accepts EITHER snake `account_id` or camel `accountId` (null/""/"cash" → Cash, uuid → validated bank — **gotcha: the existing PUT contract is snake_case keys**; reading only `body.accountId` made the modal's Cash choice a no-op and empty-patch UPDATEs fail with "Cannot coerce the result to a single JSON object"), resync re-posts using the stored `account_id`; GET list SELECT includes `account_id`. FinanceView fetches `GET /api/settings/bank-accounts` on load, "Pay from account" select (Cash · `Bank • Acct (No)`) under Payment method with helper text, edit pre-selects stored account, View-details modal shows "Paid from". Other-income untouched (no dropdown — money in). Import (CSV) still posts with no accountId (method-derived). Smoke `expense-account-smoke.mjs` 19/19 vs prod: bank choice debits that bank, cash choice (even with method card) debits Cash, no accountId + transfer → first active bank, unknown bank 400 (POST+PUT), PUT keeps/re-swaps account with ledger resync, delete removes ledger posting, ledger 0 rows at end, audit rows cleaned, fixture bank + QA user removed, user's 3 real banks untouched.
- **Income-side "Deposit into" selectors (Aug 13)** — follow-up to the expenses "Pay from account" work; the same `resolvePayingAccountId` pattern now also covers receipts: **migration `0077_other_income_account_id.sql`** (APPLIED, mirrors 0076: `other_income.account_id uuid → hospital_bank_accounts` ON DELETE SET NULL + index + non-cash backfill to first active bank). **Other income**: POST accepts camel `accountId`, PUT accepts `account_id`/`accountId` (explicit null/""/"cash" → Cash — the resolver's null-means-default shortcut must NOT be used on PUT, empty-patch UPDATEs 400 "Cannot coerce the result to a single JSON object"); GET lists include `account_id`; resync re-posts from stored account. **Billing record payment** (`/api/payments/record`): body accepts `accountId` (resolved once per run, used for every allocation's ledger 'in' row). **Lab walk-in** (`/api/lab-requests/walk-in`): body accepts `accountId`; **gotcha: resolve the account at the TOP of the route before the patient fast-create** — resolving inside the offline-payment branch left an orphan patient + request on a bad-bank 400 (observed: PT-0004 QAW3 Bad leftover; also lab_requests.created_by blocks deleting QA staff users — clean lab_requests before the users mirror). Paystack webhook still channel-derived (no user in the flow). UI: `finance-view.tsx` dropdown now on BOTH kinds — "Pay from account" (expenses) / "Deposit into" (income), edit pre-selects stored account, details modal shows "Paid from"/"Deposited into"; `billing-view.tsx` InvoiceDetailModal gained a "Receipt into" select beside Method; `lab-view.tsx` walk-in section gained "Deposit into" (shown when payMethod ≠ cash, wired via state not FormData — `accountId: payMethod === "cash" ? "cash" : payAccount`). **Fixture gotcha**: `invoices` has NO `kind`/`source` column (billing-view derives `kind` client-side) — insert fixtures without them or PostgREST 400s. Smoke `income-account-smoke.mjs` 27/27 vs prod: income bank/cash/default-bank postings + PUT resync + 400s, record bank/cash + 400, walk-in bank (priced lab_service fixture needed — serviceName-only items 400 "No catalogue price found") + cash + 400, ledger clean, full cleanup (lab_requests → invoices → patients → ledger → bank → lab service → users+auth in that order); expense smoke still 19/19.
- **CSV import display bug: "Import complete — 0 created, 0 failed" (Aug 13)** — `patient-actions.tsx` read `body.created`/`body.errors` straight off the JSON, but every API wraps results in the `ok()` envelope `{ success, data: { created, errors } }` → the client showed `0 created, 0 failed` on EVERY import while the server silently created all patients. Fixed to read `body.data.created`/`body.data.errors` (only importer with the bug — all other views use the envelope). Verified e2e: the user's exact template file content (BOM + CRLF + padded cells) → client parse 4 data rows → server 200 `{created: 4}` → client-equivalent display "4 created, 0 failed" → 4 patients in DB with generated numbers + parsed dates (4/12/1990 → 1990-04-12); cleanup left ZERO test rows (`import-repro2.mjs` 5/5). No server change needed — the import route was already correct.
- **Patients list auto-refresh on dependant add/edit/remove (Aug 13)** — the `/app/patients` list is a server component; Add patient already called `router.refresh()` after POST, but the patient-detail modal's dependant actions (POST/PUT/DELETE `/api/dependants`) only reloaded the modal via `load()` — new/edited dependants stayed invisible in the Patients list (incl. the Dependants tab) until a manual page refresh. Added `router.refresh()` after `load()` in `addDependant`/`updateDependant`/`removeDependant` (patient-dialog.tsx). Build green, deployed; bundle verified (`.refresh()` call sites present next to both dependant-save flows).
- **Pharmacy counter payments now have a Cash/Drop-into-account selector (Aug 13)** — follow-up: pharmacy previously had NO cash option (every payment, cash included, credited the first active bank by design). Now `pharmacy-billing-view.tsx` ConvertSaleModal (walk-in channel) has a "Deposit into" select — **Cash (default) first**, then the tenant banks; the selector always renders (Cash at minimum, even with zero bank accounts). `POST /api/pharmacy/payments` accepts `bankAccountId: "cash"` → ledger row `account_id NULL` (cash side); explicit uuid → that bank; missing/null → legacy default (first active bank) so old callers are unchanged. Banking view already merges pharmacy rows with NULL account_id into the Cash column. Smoke `pharmacy-cash-smoke.mjs` 11/11 vs prod (cash→NULL credit, bank→chosen bank, missing→Zenith default, invoices auto-closed paid, no extra rows). PostgREST-ilike gotcha: ledger `reference` is NULL for split payments — the invoice number lives in **`source_ref`** (a QAPX-% sweep on `reference` matched nothing); orphan sweep must filter `source_ref`. QA fixture invoices: `pharmacy_invoices` created_by FK → **auth.users**; smoke cleanup order ledger→invoices→bank→users→auth; crashed-run orphan ledger rows cleaned via source_ref sweep.
- **Billing print letterheads + Expenses/Other-Income View buttons (Aug 12)** — follow-up to the filter batch:
  - **Invoice/receipt PDF print now branded**: the invoice print page `/app/billing/invoice/[id]/print` (`components/dashboard/invoice-print-view.tsx`, linked from every Billing row + detail modal "PDF"/"Print / PDF" buttons, `?kind=pharmacy` for pharmacy invoices) prints: **TenantLetterhead first** (logo → hospital name → address → "Tel: … • Email: … • <slug>.skycare.app"), then a centered "Invoice / Receipt" title, then invoice number/issued/status/patient/items/Subtotal/Total/Paid/Balance + payments list + "Thank you for choosing…". Same letterhead on `/patient/receipt/[id]` and `/app/lab/receipt/[id]` and the pharmacy sales-modal window.print. The plumbing (`api/tenant/branding` GET — any authenticated user, reads tenants logo_url/name/address/city/state/country/email/phone + website from slug/domain; `lib/use-tenant-branding.ts` hook; `components/print/tenant-letterhead.tsx` with initial-letter fallback) was already in the working tree (uncommitted) — this session added the "Invoice / Receipt" title to invoice-print-view + patient-receipt to match the requested layout exactly, rebuilt, redeployed.
  - **Live-verified against the user's own data**: tenant `fe46a3bb` = "Liamsfield Hospitals", logo at `avatars/tenants/fe46a3bb-…/logo-1785967433770.png`, address "265 Admiralty Way, Lekki Phase 1, Lekki, Lagos, Nigeria", Tel +2348157377000, hello@liamsfieldshospital.com, website `smoketest.skycare.app` — the exact header the user requested. CPX-2026-0023 (Bukola Babafenwa, ₦500 Amaryl, paid) detail 200 → the print page for it renders 200.
  - **Expenses + Other Income view buttons** (`components/dashboard/finance-view.tsx`): every row's ACTIONS now has an Eye "View details" button BEFORE the edit pencil, opening a read-only modal (description, date, amount, category chip, payment method, vendor/source, notes) — shared component wired for both pages.
  - **Suppliers & Procurement tabs** — all four tabs (Suppliers, Balances, Purchase Orders, Payments) ALREADY carry a search + From/To `FilterBar` (client-side, export/visible-rows aware: Suppliers filters createdAt, Balances lastBoughtAt/lastPaidAt, POs createdAt, Payments paidAt) — no change needed, confirmed in source + built.
  - QA: temp hospital_admin `qacheck.*@qa.local` created on `fe46a3bb` (staff passwords are user-set & unknown — only pttrans was known), used to verify branding 200 / expenses+other-income+summary+invoices 200 / all four app pages 200, then **deleted** (users row + auth account). Bundle checks: "Invoice / Receipt" + "View details" present in deployed chunks.

### Completed
- **Vendor's module wired into Suppliers & Procurement (Aug 11)** — the existing but orphaned `frontend/src/components/dashboard/vendor-purchasing.tsx` (BalancesTab, PurchaseOrdersTab, PaymentsTab — built against migrations 0040 + 0066 and the procurement APIs, never imported by any page) is now live on `/app/pharmacy/suppliers`:
  - `pharmacy-procurement.tsx` restructured from stacked sections into a 5-tab layout: **Suppliers** (existing SuppliersTab), **Balances** (per-supplier total ordered / bought / paid / outstanding via `GET /api/pharmacy/procurement/summary` → `pharmacy_supplier_balances` RPC; PO + payment counts, last bought/paid, CSV/PDF export), **Purchase orders** (full lifecycle draft → sent → approved → received; New order / Send / Approve / Receive goods / Cancel; detail modal with GRNs; status filter; CSV/PDF), **Payments** (record payment by bank transfer / cash / POS / credit note — bank-transfer posts to the Banking ledger via `resolveBankAccountId` + `postBankLedger`; per-supplier outstanding hint + "against order" picker; CSV/PDF), **Auto-reorder** (existing PharmacyAiView).
  - Fixed to compile: added the required `templateFilename` prop to the three `ImportExportMenu` call sites; `GET /api/pharmacy/procurement/summary/route.ts` `reduce` needed an explicit generic (TS inferred `unknown` from `Array<Record<string, unknown>>`).
  - **Verified**: `npx next build` 0 errors (had to delete a stale `.next` first — type-check failures in `.next/dev/types/*.d.ts` were cached artifacts, not source errors). No migration needed; backend was already deployed (migrations 0040 + 0066).
- **HR Management module (committed `31599b1`)** — full HR suite extending (never modifying) staff/attendance/staff_leave/duty_roster:
  - **Migration `0059_hr_module.sql`** (applied to prod): `staff_profiles` (1:1 staff: hire_date, salary_grade, bank details, `credentials_status` recomputed by trigger from `staff_credentials`), `shifts` (templates; unique `(tenant_id, lower(name))` index; ward FK), `staff_shifts` (roster; UNIQUE staff_id+shift_date; `trg_staff_shifts_conflict` blocks any date already in `duty_roster`), `leave_balances` (UNIQUE staff_id+leave_year+leave_type; `hr_sync_leave_balances` seeds annual 21/sick 10/emergency 3/study 5, used = approved `staff_leave` days), `payroll_records`+`payroll_lines` (UNIQUE staff_id+pay_period; auto-rebuild on re-run), `staff_credentials`, `roles_permissions` (UNIQUE tenant_id+role; seeded `hr_seed_role_permissions`: super_admin/hospital_admin `{"*":true}`, hr_officer `{"hr.*":true}`, accountant `{"hr.payroll.view":true}`). RPCs: `hr_init_profiles` (idempotent), `hr_assign_shift` (clinical staff must hold a verified, unexpired credential; double-booking + duty_roster conflict guards), `hr_clock_in/out` (upsert attendance ON CONFLICT user_id+work_date; late/OT computed in **Africa/Lagos** timezone — attendance stored UTC), `hr_mark_missed_shifts` (scheduled→missed + absent rows, idempotent), `hr_run_payroll` (skip staff with no shift AND no attendance; base/30 per absent day; OT = 1.5x hourly past shift end, long-shift aware; returns generated/skipped/total_net), `hr_dashboard` (staff census, attendance today/month + coverage, leave balances sorted by used/entitled, payroll summary, shift coverage, credential compliance expiring ≤30d). All RLS-scoped: staff read org-wide; write = hospital_admin/hr_officer/super_admin; own payroll line self-view.
  - **API layer** (`src/lib/hr-perms.ts` + `src/app/api/hr/*` — 19 routes): staff (+lazy profile init), shifts, roster (+assign), attendance (+clock, +mark-missed), leave-balances, payroll (+[id] adjust/status, +export CSV), credentials (+verify), dashboard, permissions (matrix GET/PUT). All `withStaff`, `requireTenant`, `logAudit`, service client; `[id]` via `pathname.split("/").pop()`. Attendance route works around the table having **no staff FK** (fetches staff map, joins by user_id, attaches synthetic staff object); `users` embed returns object not array → `as unknown as` casts.
  - **UI**: HR nav group (`Clock` icon; dashboard — all; staff/roster/credentials — HR_ADMIN; payroll — HR_FINANCE incl. accountant) + 6 views: `hr-dashboard-view` (KPI cards, credential compliance alerts), `hr-staff-view` (search/filter, credential badge, detail modal w/ payroll + balances), `hr-roster-view` (monthly grid, assign/new-shift modals, status/delete), `hr-attendance-view` (date picker, clock in/out card, mark-missed for admins), `hr-payroll-view` (period picker, run/adjust/approve/paid, payslip modal, CSV), `hr-credentials-view` (add/verify/delete, expiry badges).
  - **Verified**: `hr-e2e.cjs` 46/46 vs prod (profiles, shift/defer guards incl. duty-roster conflict + expired credential, clock-in late at 08:14 WAT, auto-miss, leave balances 21/10/3/5 with used=3, payroll nurse net 292812.50 / 3 generated + 1 skipped, idempotent re-runs, dashboard rounds), tsc clean, `next build` green.
- **Doctor-role validation fix (`524df44`)** — `POST /api/prescriptions` + `lab-orders` rejected any clinician except `doctor`/`hospital_admin` while the UI dropdown lists all `CLINICIAN_ROLES` → "Invalid doctor selected" for medical_officer/surgeon/etc; both now accept `["hospital_admin", ...CLINICIAN_ROLES]` (matches appointments). **Prescription fan-out (`0060_rx_mail_fanout.sql`)** — `notify_prescription_event` additionally posts **Internal Mail** (sender = prescribing doctor): staff copy to pharmacists+hospital_admins on created/processing (in-house), patient copy to the portal account holder on every event; 10-min (sender, subject, recipient) dedupe; probe 8/8.
- **Prescriptions list fix (`b78eca6`)** — `GET /api/prescriptions` + `/[id]` embedded bare `users(...)` but prescriptions has TWO FKs to `users` (doctor_id + dispensed_by) → PostgREST PGRST200 rejected the whole query → list was **100% empty** (new + old). Switched to `users!prescriptions_doctor_id_fkey(...)`; CreateRxModal AI-added rows now always render the drug name (`value = item.medicationName` when catalog-locked) and free-text edits sync `medicationName` instead of silently dropping blank rows at submit.

### Completed
- **Batch 8 — Patient portal + staff/patient separation + Reports split**: (1) **Patient view "not found" fix** — `GET /api/patients/[id]` embedded `dependants:patients!patients_primary_account_id_fkey(...)` which PostgREST could not resolve (PGRST200 — the self-FK hint name was wrong), nulling the whole select → 404 "Patient not found" for every patient. Correct embed is `dependants:patients!primary_account_id(...)` (Column/FK hint, no constraint-name suffix). Verified against live spec + smoke. (2) **Staff list leaked patient portal accounts** — `GET /api/admin/users` returned ALL tenant users incl. `patient_api` (the auto-created patients appeared as staff cards). Added `.neq("role", "patient_api")` so patients never appear on the staff page; the existing `GRANTABLE_ROLES` gate + `PATCH /[id]` validation already prevented assigning patient accounts a staff role. (3) **Patient CRUD (Life Blossom parity)** — `PatientViewButton` modal now has Edit details, **Deactivate/Activate** (PUT `status: inactive|active`, audited) and **Remove** (DELETE → soft-delete status `transferred` + disables `users.is_active` when a portal login exists) buttons; `PatientDetail` gained `user_id`. (4) **Reports split** — renamed the existing Reports nav item to **"Medical Report"** (`/app/reports`, unchanged — patient/clinical reports; patients already read their own via `/patient/records` + `/api/medical-reports` family gate) and added a new **"Financial Report"** nav item for `ADMIN_ROLES` at `/app/financial-reports` → `components/dashboard/financial-reports-view.tsx` (modeled on life-blossom's `.app/admin/reports` page but themed with SkyCare tokens + SkyCare APIs): month picker, KPI cards (total revenue/other income/new patients/net), 12-month recharts revenue LineChart, income-by-source Pie, date-windowed **Profit & Loss statement** (16 expense lines keyed off `description` regex + `EXPENSE_CATEGORIES`, net in emerald/rose), CSV export + window-print P&L with letterhead (tenant logo/name/address). Data: `GET /api/invoices` (paid/partially_paid `total_amount` for medical revenue), `/api/other-income`, `/api/expenses?from&to`, `/api/patients`, `/api/tenant-settings` (org identity).
  - **Verified**: `npx next build` passes (0 errors); prod restarted on :3000; `menu-smoke.mjs` 21/21, `batch4-smoke.mjs` 36/36, `patient-portal-smoke.mjs` 15/15; targeted checks — staff roles now `doctor, hospital_admin` (patient_api excluded), patient detail 200 + dependants array, status toggle round-trip (inactive→active), patient hitting `/api/admin/users` = 403, `/app/financial-reports` 200 with invoices/expenses/other-income/tenant-settings all 200.
- **Patient dashboard modernization + tenant logo resize + staff dark-mode confirmation** — (1) `/patient` overview (`components/patient/patient-dashboard.tsx`) redesigned: gold gradient hero with greeting-by-time-of-day + quick actions (Book appointment / Pay a bill / View results) + date/avatar chip; 3 stat cards (upcoming, outstanding, results ready) with icon chips + hover lift; recharts donut "Your balance" (Paid vs Outstanding, center label, overdue notice); appointment list with date tiles + Dr name + time + status pill; bills list with paid-percentage progress bars; "Care snapshot" summary row. All theme-token based so it follows light/dark. (2) Tenant logo in staff topbar (`topbar.tsx`) AND patient header (`patient-shell.tsx`) shrunk `max-h-[68px] max-w-[68px] rounded-xl` → `max-h-9 max-w-9 rounded-lg` to match the 36px `SkyCareMark` in the sidebar. (3) Staff dark mode lives in Account page (`/app/account` → `account-view.tsx` "Display preferences" Light/Dark, live-apply + persisted to `users.preferences.jsonb`) and in both avatar dropdowns via `ThemeToggle` — verified present in built bundle.
  - **Smoke note**: SSR HTML for `/patient` and `/app/account` shows only loading shells (client components fetch data after hydration) — assert on built JS bundle strings (e.g. "Patient overview", "Dusk") instead of server HTML.
  - **Fixture gotcha**: test patient `pt.123602@skycare.app` was silently promoted to `hospital_admin` (both `users.role` and auth `app_metadata.role`) — caused batch4 permission assertions (patient POST announcement/leave/bank-accounts 403) to fail with 201. Fixed by service-client update back to `patient_api` in BOTH places (users table + `auth.admin.updateUserById` app_metadata). If permission smokes start failing again, check `GET /rest/v1/users?email=eq.pt.123602@skycare.app` role first.
- **Dark mode (ALL USERS) + patient avatar dropdown + patient Account/Download pages** — global theming with the SkyCare sky palette (light) and the life-blossom "Dusk & Gold" scheme (dark): `#0a0f1a` navy / `#e0a84a` gold. `globals.css` has an `html[data-theme=dark]` token block (overrides `--color-primary`→gold, `--color-background`/`--color-foreground`, muted, border, shadow) plus ~60 utility remaps (`bg-white`→`#0f1730`, `bg-slate-50`→`#0e162a`, `text-slate-700`→`#ccd3e0`, `border-slate-200`→`#20304a`, `hover:bg-slate-50`→`#162033`, semantic chips, etc.). FOUC guard = `next/script` `beforeInteractive` in `src/app/layout.tsx` sets `documentElement.dataset.theme = localStorage.getItem("skycare-theme") || "light"` (storage key `skycare-theme`); `suppressHydrationWarning` on `<html>`/`<body>`. Runtime helper `lib/theme.ts` (`readStoredTheme`/`applyTheme`/`watchTheme`, dispatches `skycare:theme-changed`). `ThemeToggle` (`components/theme-toggle.tsx`, compact + labeled variants) applies theme locally AND persists via `PUT /api/account/preferences`.
  - **Source of truth = `users.preferences.jsonb`**; `GET|PUT /api/account/preferences` now allows ALL roles via `withAuth` (was staff-only) so patients can persist theme + push prefs; PUT validates theme ∈ THEMES (bad value → 400).
  - **Account preferences page** (`components/dashboard/account-view.tsx`) gained a "Display preferences" card with a Light/Dark segment control (`Sun`/`Moon`, `aria-pressed`) that applies the theme live on click, not just on save.
  - **Patient user menu** (`components/patient/patient-user-menu.tsx`): avatar + chevron in `patient-shell.tsx` header → dropdown with Profile `/patient/profile`, Account `/patient/account`, Download SkyCare app `/patient/download`, Dark-mode toggle, Sign out. Staff dropdown (`components/dashboard/user-menu.tsx`) gained the same Dark-mode toggle row.
  - **New pages**: `/patient/account` reuses `AccountView`, `/patient/download` reuses `DownloadView (both `force-dynamic`). Push subscribe/unsubscribe/vapid endpoints all moved to `withAuth` so patients can receive push.
  - **Verified**: build passes (Next 16.2.12); smoke `patient-portal-smoke.mjs` 15/15 (patient shell + theme init script + account/download pages 200 + prefs GET/PUT for patient + bad-theme 400 + vapid key), regression `menu-smoke.mjs` 21/21, `batch4-smoke.mjs` 36/36. Built CSS verified to contain `html[data-theme=dark]` + token/utility overrides. Theme-check gotcha knock-on: Smoke assertions asserted on SSR HTML for `/patient/account` found only "Loading…" (client fetch) — assert on bundle strings / API instead.
- **Patient doctor-note editing (batch 7.5)** — the patient portal (`/patient/records`) was read-only; patients can now edit their own notes. `PUT /api/doctor-notes/[id]` accepts `patient_api`: own-family + non-confidential only (else 404), and ONLY content fields (`vitals`, `testsProcedures`, `clinicalFindings`, `diagnosis`, `medications`, `treatmentRecommendations`, `nextVisitDate`, `nextVisitReason`) — `isConfidential`/`visitDate`/`appointmentId` from a patient return 400, doctor_id is never client-settable; staff PUT unchanged. UI: `patient-records.tsx` gained a pencil button per note + `NoteEditModal` (same 7-section form as staff minus visit date/confidential toggle; amber note "edits are recorded with your patient account"). Audit: edits logged with role `patient_api` + user_id via `logAudit`. Batch 7 smoke extended to 77/77 (incl. confidential 404, cross-tenant 404, staff unaffected, edit survives reset→restore cycle, post-restore edit works); batch 4 still 36/36.
- **Batch 7** — system backup/reset/restore + landing/doctors parity. 58/58 smoke PASS; batch 4 regression 36/36.
  - **Landing/doctors parity** — the `landing_doctors` table existed but was orphaned (no API/UI, `[slug]` page had no doctors section). Now: `GET/POST /api/landing/doctors` (ADMIN_ROLES; GET sorted by `sort_order, name`; POST validates name + unique-per-tenant name; both audit-logged) and `PUT/DELETE /api/landing/doctors/[id]` (tenant-scoped, audit-logged; uses the `idFrom(req)` pattern â€" `resolveParam(params)` broke on Next 16, extract id via `split("/").filter(Boolean)` `[len-1]`). Admin UI: `components/dashboard/website-doctors-section.tsx` card in `/app/settings` (list, add/edit dialog with photo upload + preview, sort-order edit, active toggle). Public site: `src/app/[slug]/page.tsx` now server-renders a doctors section (service client, tenant-filtered by slug; empty = section hidden). Migration `0011_landing_doctors_public_fix.sql` removes the anon SELECT RLS policy + anon grant (anonymous visitors could list every tenant's doctors) — **APPLIED** (pushed via CLI; verified live: anon GET/POST/PATCH/DELETE on `landing_doctors` all 401).
  - **`GET /api/system/backup`** (ADMIN_ROLES, caller's tenant only) — streams a JSON backup `{ version: 1, createdAt, tenantId, tenant, tables }`; `tables` covers the 20 data tables (`BACKUP_TABLES` incl. branches, staff, dependants, landing_doctors, audit_logs, security_events) PLUS child tables of parents-without-children cascades (`PARENT_CHILD`: `prescription_items → prescriptions`, `invoice_items → invoices`, `internal_message_recipients → internal_messages`, `push_subscriptions → users`); `Content-Disposition: attachment; filename=skycare-backup-<tenant>-<ts>.json`.
  - **`POST /api/system/reset`** (ADMIN_ROLES) — wipes ALL data rows (17 wipe tables via service client with `.select("*")` — see chat_presence gotcha), deletes every auth account of the tenant EXCEPT the caller, keeps config (tenant profile/settings, branches, bank accounts). Returns `{ deleted: { users, patients, ... }, kept: [...] }`.
  - **`POST /api/system/restore`** (ADMIN_ROLES) — validates `version === 1`, wipes, then: branches first (`idMap` backup→live; users.branch_id FK), users (insert or re-auth — caller's profile row is UPDATED in place, accounts whose email changed are recreated with a temp password and listed in `users.createdAccounts`), then the remaining 18 tables in `ORDER` with per-table column whitelists (`T`) and FK remap maps (`REMAP`: users/patients/invoices/payments/prescriptions/medical-records/appointments/notifications/expenses/other-income/internal-messages/internal-message-recipients/chats/chat-messages/chat-presence/duty-roster/staff-leave/staff/landing-doctors/audit-logs/security-events/dependants — `payments` has NO branch_id, `medical_reports` has NO updated_at), `tenant_id` forced only when the column exists, 500-row chunks. Returns `{ wiped, restored: {...counts}, users: { createdAccounts, reusedAccounts, note } }`. Restored auth = temp passwords → admin must reset from Settings → Users (response says so).
  - **Admin UI** — `components/dashboard/system-backup-section.tsx` card in `/app/settings` (ADMIN_ROLES only): Download backup / Restore from file (confirm dialog, temp-password notice) / Reset everything (type-to-confirm, keeps-caller + config notice).
  - **Batch 7 smoke**: `C:\Users\Admin\AppData\Local\Temp\opencode\batch7-smoke.mjs` — 58/58 PASS (role gates doctor→403 on all 4 routes; landing doctor CRUD + public site render/hide/show; backup attachment + all child tables; reset keeps caller + deletes wiped auth account + keeps bank accounts; restore full round-trip incl. branches/users/patients/invoices/payments/staff/audit_logs/landings counts, patient/doctor re-login with fixed temp passwords, role gates still enforced). Must run from `frontend\`; reads `.env.local`; the public-site fetches use `x-forwarded-host` (see gotcha); `batch7-restore-runner.mjs` + `batch7-colcheck.mjs` (PostgREST column-whitelist verification against live spec) + `batch7-backup.json` live in the same temp dir.
- **Batch 6 (committed `05ea048`)** — online payments (Paystack), per-tenant keys, placeholder-gated. 26/26 smoke PASS; batch 4 still 36/36.
  - **`frontend/src/lib/paystack.ts`** — hand-rolled fetch client (no SDK; BASE `https://api.paystack.co`): `getPaystackKeys(svc, tenantId)` reads `tenants.settings.paystack` JSONB `{ publicKey, secretKey, webhookSecret }` with env fallback (`PAYSTACK_SECRET_KEY`, `PAYSTACK_WEBHOOK_SECRET`, `NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY`); `isPlaceholderKey()` (empty / <15 chars / `placeholder` / `PAYSTACK_SECRET_KEY` / `${` are placeholders; `sk_live_`/`sk_test_`/32+ hex are real — NOTE the inversion bug I hit: sk_test_ keys must return FALSE); `initializeTransaction` (amount in kobo, reference, metadata, callback_url), `verifyTransaction`, `verifyWebhookSignature` (HMAC-SHA512 constant-time), `generateReference` (`SC-<tenant8>-<rand>`), `maskKey`.
  - **`POST /api/payments/initialize`** (patient_api only; body `{ invoiceId, amount }`) — same family/invoice checks as declare (dependant 403, paid/cancelled/refunded rejected, amount ≤ outstanding). No tenant keys → `{ enabled: false }` (client falls back to offline declare). With keys → creates a Paystack checkout, metadata carries `{ tenant_id, invoice_id, patient_id, invoice_number }`, returns `{ enabled: true, authorization_url, access_code, reference }`. No payment row is written at initialize (webhook is the single writer — keeps idempotency clean).
  - **`POST /api/payments/webhook`** (public, no auth) — reads RAW body; tenant resolved from `data.metadata.tenant_id` BEFORE trusting payload, signature verified against THAT tenant's webhook secret (cross-tenant forgery fails); env secret fallback; payment-row lookup fallback for tenant. Only `charge.success`; idempotent via partial unique index on `payments.reference` (23505 race → `{ duplicate: true }`); inserts payment `status: completed`, `gateway: "paystack"`, method card/transfer by channel, kobo→Naira, metadata stores channel/card_type/last4/bank/fees/paid_at; updates invoice `paid_amount` + status (paid/partially_paid); notifies hospital_admin+cashier (`payment_confirmed`); direct audit_logs insert with `role: "paystack_webhook"`, `user_id: null` (webhook has no ctx). Errors: 400 empty body/missing metadata/placeholder key, 401 bad signature, 400 invalid JSON, 500 insert failure.
  - **`GET /api/payments/callback`** — redirect target; resolves reference/trxref, loads tenant from the payment row when recorded (webhook wins), verifies with Paystack when keys configured, redirects `/patient/billing?paystack=success|failed|processing|error&reference=…`. If webhook hasn't landed yet → `processing` (UI auto-refreshes).
  - **`GET /api/payments/gateway-status`** (any role, requires tenant) — `{ enabled, provider, source, publicKeyMasked }`, no secrets.
  - **Tenant settings** — `PUT /api/tenant-settings` accepts `settings.paystack` (partial update, blank string = clear key, per-key merge into existing paystack object; publicKey must look like `pk_(live|test)_`, secret/webhook validated min 8 chars); `GET` redacts: returns `{ publicKey, secretKeyConfigured, webhookSecretConfigured, configured }` — raw secrets NEVER leave the server. `tenant-settings-view.tsx` gained an "Online payments (Paystack)" section: Active/Not-configured badge, three inputs (blank = keep current, save blank = remove), webhook-URL hint. Edits are tracked per-field so untouched keys aren't re-sent.
  - **Patient billing UI** — `patient-billing.tsx`: fetches gateway-status on mount; DeclareModal gains "Pay online (card)" option (default when enabled); online submit → initialize → `window.location.href = authorization_url`; banner reads `?paystack=` (success/processing/failed) and strips the param via `history.replaceState`; invoice list auto-reloads after return. Bank transfer/POS unchanged (still pending until staff confirm).
  - **Batch 6 smoke**: `C:\Users\Admin\AppData\Local\Temp\opencode\batch6-smoke.mjs` — 26/26 PASS (gateway default disabled; initialize 404 unknown invoice; PUT paystack keys → GET shows configured WITHOUT leaking secrets; gateway enabled for patient; signed `charge.success` webhook → payment completed/gateway paystack/Naira amount/method card + invoice paid + idempotent replay `existing: true` + no dup rows + bad signature 401; callback 307 → /patient/billing (test uses `redirect: "manual"` — plain fetch auto-follows); patient invoice list shows the completed payment). Must run from `frontend\`; reads `.env.local` itself; cleanup deletes test invoice+payment.
- **Batch 5 (committed `25202e5`)** — small items from the life-blossom gap list: Billing summary API + cards, doctor-notes/[id] CRUD + staff UI, internal mail delete, staff onboarding edit, patient notifications page.
  - **Billing summary** — `GET /api/billing/summary?month=YYYY-MM` (hospital_admin/cashier/super_admin only, patient 403): returns `{ collected, outstanding, monthCollected, monthOtherIncome, monthTotal, invoiceCount }` from 5 parallel service queries (completed payments all-time; pending+partially_paid invoices outstanding; month payments by `paid_at`; month `other_income` by `income_date`; invoice count). `billing-view.tsx` fetches it in `load()` (Promise.all) and shows a 4-col summary row (Outstanding amber, Collected emerald, This month incl. other income sky, Awaiting confirmation = pending count).
  - **Doctor notes staff UI** — `components/dashboard/doctor-notes-section.tsx` added to the patient detail modal (`patient-dialog.tsx`): expandable note cards (vitals, tests/procedures, findings, diagnosis, medications, recommendations, next visit, confidential badge) + clinician-gated New note / Edit / Delete. Form posts camelCase to `POST /api/doctor-notes` (adds `patientId`) and `PUT /api/doctor-notes/[id]`; `isClinician` from `/api/auth/me` claims.role (hospital_admin/super_admin/doctor/nurse). The `[id]` API (GET staff-any-tenant / patient own-family non-confidential else 404; PUT/DELETE clinical-only, camelCase body, audit-logged) was built in this batch.
  - **Internal mail delete** — `DELETE /api/mail/[id]?view=inbox|sent` (withAuth; inbox default = recipient-row id, own copy only via `.eq("recipient_id", me)`; sent = message id, sender-only, recipients cascade via FK ON DELETE CASCADE; 404 for non-owner; audit-logged). Trash buttons added to `dashboard/mail-view.tsx` and `patient/patient-mail.tsx` (row buttons restructured as sibling buttons — no nested `<button>`). PostgREST embed of `internal_messages` on a recipient row is an ARRAY (`row.internal_messages?.[0].subject`), not an object.
  - **Staff onboarding edit** — staff cards in `staff-management.tsx` gained "Edit details" (only when user has a `staff` row) opening a modal for department, specialization, license_number, qualification, employment_type (full_time/part_time/contract/locum), years_of_exp, base_salary, is_available → `PUT /api/staff/[id]` (admin-only). `GET /api/admin/users` staff embed extended to include qualification/employment_type/years_of_exp/base_salary.
  - **Patient notifications** — `/patient/notifications` page (`components/patient/patient-notifications.tsx`, no admin composer): All/Unread tabs, mark-read/delete/mark-all-read. Bell already linked there via `basePath="/patient"`; added Bell nav item to `patient-nav.ts` (11 items).
  - **Batch 5 smoke**: `C:\Users\Admin\AppData\Local\Temp\opencode\batch5-smoke.mjs` — 15/15 PASS (billing summary admin 200 + patient 403; doctor-notes POST/GET/PUT/DELETE + patient confidential 404; mail sent-delete cascade + recipient inbox-copy delete + non-sender 404; staff PUT dept/specialization + admin/users staff fields; /patient/notifications page 200 + notifications list). Must run from `frontend\` (imports supabase-js); reads .env.local itself. Batch 4 script still 36/36 after batch 5.
- **Batch 4 (committed `e541e0a`)** â€" Notifications center, Staff leave, Duty roster, Bank accounts: APIs + UIs as detailed above; build passes; 36/36 smoke pass; `nav.ts` added Leave (`CalendarDays`, all staff) + Roster (`CalendarRange`, all staff); bank accounts shown in settings + patient declare-payment modal.
- **Tenant onboarding & auth** â€” signup edge function creates hospital_admin + tenant claims; login redirects by role (`patient_api` â†’ /patient, else /app); disabled accounts bounced at login + app layout; idle auto-logout 15 min â†’ POST /api/auth/logout (audit entry).
- **Admin/staff management** â€” `/app/staff` (hospital_admin/super_admin): create admins/staff with direct credentials (`POST /api/admin/users` via `svc.auth.admin.createUser` + users mirror + staff row; `GRANTABLE_ROLES` excludes super_admin), role change with app_metadata sync, reset password, activate/deactivate, self/super_admin guards.
- **Patients & family** â€” `/app/patients`: register (auto `PT-` number, optional portal login), view/edit, dependants add/remove (cap 5), medical records section (add w/ confidential flag). APIs: `api/patients(+[id])`, `api/dependants(+[id])`, `api/medical-records(+[id])`.
- **Appointments** â€” `/app/appointments`: create (patient/doctor/type), status actions (confirm/start/complete/cancel), patient portal notifications. APIs: `api/appointments(+[id])`.
- **Billing** â€” `/app/billing` (admin/cashier): create invoice (dynamic items + VAT + discount, `INV-` number), status filters, detail view with payment history, record payment (multi-invoice allocation, auto invoice paid/partially_paid sync, confirms patient's pending declaration in place), pending-declaration confirm/decline panel. APIs: `api/invoices(+[id])`, `api/payments`, `api/payments/record|declare|cancel` (+`api/expenses(+[id])`, `api/other-income(+[id])`).
- **Pharmacy** â€” `/app/pharmacy` (clinical): prescriptions list/filters, create (dynamic medications), per-item dispensing w/ auto status. APIs: `api/prescriptions(+[id])`.
- **Laboratory** â€” `/app/lab`: orders + status flow (requested â†’ sample_collected â†’ in_progress â†’ completed), per-test results (result/unit/abnormal), test catalog manager. APIs: `api/lab-tests(+[id])`, `api/lab-orders(+[id])`.
- **Audit & security UI** â€” `/app/audit-logs` (hospital_admin/super_admin; nav item w/ ShieldCheck): tabs for Audit Logs + Security Events, filters (entity type/action/role/event type/severity/date range), expandable column-diff (JSON old/new) + metadata views, page-aware CSV export, pagination. APIs: `GET /api/audit-logs` (tenant-scoped; filters entity_type, entity_id, user_id, role, action, from, to) and `GET /api/security-events` (tenant-scoped **plus global tenant_id IS NULL rows** via `.or(tenant_id.eq.X,tenant_id.is.null)`, mirroring the `security_events_admin_read` RLS policy; filters event_type, severity, user_id, from, to) â€” both `withStaff` + ADMIN_ROLES check, users embed.
- **Landing/mobile** â€” responsive landing + tenant sites (mobile nav dropdowns, gallery widths, `[slug]` header truncation), green Start Free Trial buttons, patients/appointments mobile card layouts.
- **Patient portal** (`/patient`) â€” `patient/layout.tsx` (patient_api guard, only-if-active, tenant name) + PatientShell (`components/patient/patient-shell.tsx`, nav in `lib/patient-nav.ts`); pages: Overview dashboard (cards + upcoming appointments + bills), Appointments (book w/ family picker via `GET /api/patients/me`, cancel), Bills & payments (invoice detail w/ items+payments, declare-payment modal â†' `POST /api/payments/declare`), Prescriptions (items, dispensed qty), Lab results (test rows, abnormal flags, pending/ready), Family (add dependant w/ optional portal login, remove). All family-scoped via the existing APIs.
- **API role hardening** â€” staff-only routes now use `withStaff` (patient_api â†' 403): `api/staff(+[id])`, `api/patients(+[id])` (portal uses new `GET /api/patients/me` instead), `api/expenses(+[id])`, `api/other-income(+[id])`, `api/lab-tests(+[id])`, `api/medical-records` POST + `[id]`, `api/invoices` POST + `[id]` PUT/DELETE, `api/prescriptions` POST + `[id]` PUT, `api/lab-orders` POST + `[id]`, `api/appointments/[id]` DELETE. `api/dependants/[id]` PUT/DELETE gained the same family-scope guard as GET.
- **Cross-tenant isolation smoke-tested** â€” tenant A "Smoke Test Clinic" (smoketest@skycare.app, pt.*@skycare.app patients) vs tenant B "Isolation Test Clinic" (iso-123754, iso.123754@skycare.app / iso.pt123754@skycare.app): lists return own rows only; by-id reads across tenants â†' 404; patient calling staff endpoints â†' 403. Session cookies for API smoke tests are `sb-pvakwxeusbxesdealuuc-auth-token` = `base64-<base64url(JSON session)>` (see `C:\Users\Admin\AppData\Local\Temp\opencode\smoke-patient.ps1` / `smoke-isolation.ps1`).
- **Settings** â€” `/app/settings` (hospital_admin/super_admin; last `soon` nav item flipped off): Hospital profile (name/email/phone/address/city/state/country), Branding & locale (brand color hex, currency, timezone), Staff & patient numbering (patient/dependant/staff/invoice prefixes validated by `PREFIX_PATTERN` + `normalizePrefix`, SMS provider, labAutoFill). API: `PUT /api/tenant-settings` (admin-only; validates profile fields; merges prefix/sms/labAutoFill into `tenants.settings` JSONB preserving unknown keys; writes audit entry) and `GET /api/tenant-settings` (admin-only; returns profile + raw settings JSONB; UI fills defaults from `DEFAULT_TENANT_SETTINGS`).
- **Settings smoke-tested on prod**: unauth 401, patient 403, admin 200 + PUT round-trip (brand/prefix/sms/labAutoFill persist; other tenant untouched), `/app/settings` page redirects patients to /login (layout guard).
- **Expenses + Other income** â€” `/app/expenses` and `/app/other-income` (ADMIN nav items): month picker, summary cards, search + category filter, add/edit/delete dialogs, camelCase bodies to `/api/expenses(+[id])` and `/api/other-income(+[id])` (note: [id] PUT takes snake_case keys). Shared `components/dashboard/finance-view.tsx` (kind prop).
- **Profile** â€” `/app/profile` (all staff; topbar avatar links to it): avatar upload (`POST /api/uploads/avatar` to public `avatars` bucket, 2 MB cap, PNG/JPG/WebP/GIF, own-folder storage policies in migration 0010), edit name/phone (`PUT /api/auth/me` now also defines PUT alongside the existing GET), change password (`POST /api/auth/change-password` verifies current via signInWithPassword then updateUser; min 8 chars).
- **Internal mail** â€” `/app/mail` (ADMIN): tabs Inbox/Sent/Compose; broadcast-to-staff; sidebar unread badge via `UnreadMailBadge` polling `/api/mail/unread-count` every 30s. APIs: `GET/POST /api/mail/inbox`, `GET /api/mail/sent`, `GET /api/mail/recipients` (staff/patients), `PUT /api/mail/[recipientRowId]/read` (all `withAuth` since patient portal v2 — see below; staff compose still allows broadcast, patients cannot). Inserts into `internal_messages`+`internal_message_recipients`, notifies recipients, tenant-validates recipients.
- **Chats** â€” `/app/chats` (ADMIN): staffâ†"patient messaging; two-pane list+thread, presence dots, directory of portal patients for new chats, 5s message polling. APIs: `GET/POST /api/chats`, `GET/POST /api/chats/[chatId]/messages`, `POST /api/chat-presence` heartbeat (patient_api now supported since patient portal v2 — see below). Notifies the receiving party's user_id.
- **Medical reports** â€” `/app/reports` (clinical ADMINS/doctor/nurse; write = hospital_admin/super_admin/doctor): grouped-by-date list, `<details>` expand, new-report dialog. `GET/POST /api/medical-reports` (tenant-scoped `MR-####` numbering; patient_api reads own family only; READ_ROLES clinical, WRITE_ROLES admin+doctor).
- **Patient portal v2 — records + profile + receipts** â€” `/patient/records` (medical records + doctor notes + medical reports, grouped, expandable; confidential doctor notes hidden from patients via `is_confidential = false` filter in the SERVICE client — RLS does the same for direct reads, use both), `/patient/profile` (avatar upload, edit name/phone via `PUT /api/auth/me`, edit date_of_birth/marital_status/blood_group/genotype/medical_plan/address via new `PUT /api/patients/me` with enum validation, change password, sign out), `/patient/receipt/[id]` (printable invoice+payments proof; reachable via "View receipt" link added to the billing page). Doctor notes API: `GET/POST /api/doctor-notes` (READ_ROLES clinical w/ required patient_id; patient_api family-scoped, non-confidential only; WRITE_ROLES clinical; doctor_id + created_by FK hints on user embed).
- **Patient portal v2 — chats + mail** â€” `patient-nav.ts` now has 10 items (added Medical records, Chats, Messages, Profile). Chats APIs extended for patient_api: `GET/POST /api/chats` (patient sees chats whose patient_id is in their family; directory = active staff of tenant; POST body `staffUserId`), `GET/POST /api/chats/[chatId]/messages` (getChat is role-aware: staff â†' staff_user_id = me, patient â†' patient_id in family; patient send notifies staff_user_id, staff send notifies patient). Mail APIs relaxed from `withStaff` to `withAuth`: `GET/POST /api/mail/inbox`, `GET /api/mail/sent`, `GET /api/mail/recipients` (for patient_api returns `patients: []` only staff), `GET /api/mail/unread-count`, `PUT /api/mail/[recipientRowId]/read` — queries are all recipient/sender-scoped so no extra isolation needed; patient POST cannot broadcast and recipients are filtered to staff roles. Patient portal UI: `components/patient/patient-chats.tsx` (own user id pulled from `/api/auth/me` for bubble side), `patient-mail.tsx` (Inbox/Sent/Compose to staff only). Smoke-verified end-to-end incl. staffâ†"patient both directions + unread/read flags + presence.
- **Route param gotcha**: dynamic segments NOT at the end of a route (e.g. `/api/chats/[chatId]/messages`, `/api/mail/[id]/read`) have the id in the second-to-last path segment â€" use `split("/").filter(Boolean)` and index `[len-2]`, NOT `.pop()`. For a terminal `[id]`/`[recipientRowId]` route the id is the LAST segment `[len-1]` (I wrongly used `[len-2]` in batch 4's notifications/leave/roster/bank-accounts `[id]` routes â€" all hit 404 "not found" until fixed).
- **PostgREST same-table double embed**: embedding the SAME table twice via FK hints (e.g. `staff_leave.user_id` + `approved_by` both â†’ users) 400s with `table name "staff_leave_users_1" specified more than once` unless the second embed is aliased with a prefix: `approver:users!staff_leave_approved_by_fkey(...)`.
- **Notifications center** â€” shared `components/notifications-bell.tsx` (30s unread poll via `GET /api/notifications/unread-count`, dropdown unread list + mark all read via PUT per id + "View all â†'" `basePath` prop; wired into staff `dashboard/topbar.tsx` basePath `/app` and `patient/patient-shell.tsx` basePath `/patient`). Page `/app/notifications` (`dashboard/notifications-view.tsx`): All/Unread tabs, mark-read/delete/mark-all-read, admin-only announcement composer. APIs: `GET /api/notifications` (own, paginated, `unread_only`), `POST /api/notifications` (hospital_admin/super_admin broadcast to active non-patient staff, optional `role` filter via `notifyUsers`), `PUT|DELETE /api/notifications/[id]` (own rows only). Unlike life-blossom (which deletes read notifications), SkyCare keeps rows with `is_read` flag â€" API supports both patterns.
- **Staff leave** â€” `/app/leave` (all staff; admin sees "All requests" tab): request modal (5 leave types, date range, `days` is GENERATED column), cancel own pending = DELETE (staff_leave has NO `cancelled` in the status CHECK), approve/reject admin-only â†' notifies requester via `notifyUsers`. APIs: `GET/POST /api/staff/leave` (GET embeds requester `users!staff_leave_user_id_fkey` + aliased `approver:users!staff_leave_approved_by_fkey`; admin = all/status filter, staff = own), `PUT|DELETE /api/staff/leave/[id]`. All `withStaff` (patient_api â†' 403).
- **Duty roster** â€” `/app/roster`: week view (prev/next), assign-shift modal (preselects Today), per-day shift cards, delete. APIs `GET/POST /api/duty-roster` (GET by `from/to/staff_id/user_id` + `staff!duty_roster_staff_id_fkey` embed; POST upserts on `onConflict: staff_id,shift_date` UNIQUE) and `PUT|DELETE /api/duty-roster/[id]` â€" all `withStaff`, write/assign admin-only, staff can read.
- **Bank accounts** â€" `hospital_bank_accounts` CRUD: `GET/POST /api/settings/bank-accounts` (POST admin-only, max 5, 10-digit account number; GET patient_api â†' active-only), `PUT|DELETE /api/settings/bank-accounts/[id]` (admin-only). UI: `components/dashboard/bank-accounts-section.tsx` added to `/app/settings` (add / pause-toggle / delete) AND shown in patient declare-payment modal (`components/patient/patient-billing.tsx` fetches active accounts when method = bank_transfer).
- **Batch 4 smoke**: `C:\Users\Admin\AppData\Local\Temp\opencode\batch4-smoke.mjs` â€" 36/36 PASS (admin announcement â†' doctor unread â†' mark-read â†' delete; leave request â†' admin approve â†' doctor notified; roster assign/update/upsert/delete + patient 403; bank accounts add/deactivate/delete + patient active-only view). Note: `duty_roster.until_time`/`from_time` are `time` and return `"17:00:00"` (harness compares with `.startsWith`).
- **Build**: `npm run build` (frontend) passes; prod server runs `next start -p 3000` (find PID via `Get-NetTCPConnection -LocalPort 3000`). If `next start` reports "Could not find a production build", `.next` was emptied (e.g. interrupted build) â€" re-run `npm run build`. Restart gotcha: kill by the PID owning :3000 each time â€" reusing a stale PID lets the OLD process keep serving.
- **Dashboard Life Blossom parity**: `/api/dashboard` now also returns `kpis.newPatients, staffCount, appointmentsInPeriod, appointmentsOutsidePeriod, unpaidCount, outstanding, revenueTrendPct, revenueUp` (new patients & appointments-count per selected month, unpaid = invoices pending/partially_paid issued in the month, revenue trend vs previous month computed from the existing 12-month series). `dashboard-view.tsx` KPI cards now use Life Blossom-style trope text: "Total Revenue: +X.X%" (TrendingUp/Down, emerald/rose), "N staff on board", "N outside period", "N unpaid in period" — all from real tenant data. Recent patients table gained a per-row **View** eye button (`<PatientViewButton />` with a full `PatientRow` embedded by the API) opening the existing patient detail dialog. All dashboard/chart/table labels are Title-Cased ("Hospital Dashboard", "Weekly Revenue Breakdown", "Revenue Split", "Monthly Revenue Trend", "Appointments by Department", "Recent Patients", "Today's Schedule", "Quick Actions", "Net Profit / Loss"). Quick actions "Generate Reports" and "View Analytics" now link to `/app/financial-reports` (Financial Reports) instead of `/app/reports`. Sidebar labels title-cased in `nav.ts` ("Other Income", "Medical Reports", "Financial Reports") and `patient-nav.ts` ("Bills & Payments", "Lab Results", "Medical Records"). Verified: build 0 errors, `/api/dashboard` live-check 12/12, menu-clean 21/21, batch4 36/36, patient-portal 15/15, dashboard-smoke 18/18.

### Completed
- **Animated login/signup backgrounds + Google & Yahoo OAuth (Aug 6)** — (1) The light-blue gradient on `/login` + `/signup` (page `bg-gradient-to-br` AND the sky-blue illustration panel) now drifts slowly: new `.animate-gradient-slow` (background-size 200% 200%, `gradient-shift` 20s ease-in-out) in `globals.css`, applied to both `<main>` and the illustration panel. (2) Google OAuth ENABLED on the prod project (ref `pvakwxeusbxesdealuuc`) via Management API `PATCH /config/auth` (`external_google_enabled`, client id `557180180143-…apps.googleusercontent.com`, secret set; `custom_oauth_enabled=true`). (3) Yahoo is NOT a native Supabase provider → created as a **custom OIDC provider** `custom:yahoo` (issuer `https://api.login.yahoo.com`, OIDC discovery auto-resolved: `oauth2/request_auth`, `oauth2/get_token`, `/openid/v1/userinfo`, PKCE on, scopes openid/profile/email) via GoTrue admin API `POST {url}/auth/v1/admin/custom-providers` (service key MUST be sent with a non-browser User-Agent like `supabase-cli/2.110.0` or GoTrue's key-protector 401s "Forbidden use of secret API key in browser"). (4) iCloud/Apple REMOVED per request (needs $99 Apple Developer) — `OAUTH_PROVIDERS` in `login/page.tsx` now only `google` + `custom:yahoo` (labels Google / Yahoo; `ProviderLogo` switch updated; AppleLogo/ICloudLogo imports dropped). (5) Auth callback blocks social users with no role claim: everyone must map to an existing hospital account — no `app_metadata.role` → signOut + redirect `/login?error=oauth_no_account` (login page shows amber alert via `oauthErrorText()`). (6) **Env gotcha (cost me 20 min)**: shell commands that read `.env.local` WITHOUT an explicit workdir read `C:\Users\Admin\Downloads\life-blossom-hosp\.env.local` (points at a DIFFERENT project ref `hkqhsgdutaaufqqrekdx`) — the first authorize tests + the first Yahoo-provider POST hit the WRONG project (stray provider since DELETEd from `hk…rekdx` via its admin API; re-created on the correct `pv…`). `.env.local` keys (anon + service_role JWT `ref` both assert `pvakwxeusbxdealuuc`) — always run admin/auth curl probes with workdir `frontend` or hard-code the URL.
  - **Verified live**: `GET /auth/v1/authorize?provider=google` → 302 to accounts.google.com with the new client id + correct callback `https://pv….supabase.co/auth/v1/callback`; same for `provider=custom:yahoo` → 302 to api.login.yahoo.com/oauth2/request_auth (PKCE code_challenge present); password login for smoketest tenant still works (role hospital_admin); `npx next build` 0 errors; server restarted, `/login` 200 (OAuth buttons are inside `<Suspense>` → hydration-only; assert on built chunk string `custom:yahoo` instead of SSR HTML — found in `.next/static/chunks/`).

### Blocked
- (none)

## Next Move
- All life-blossom gaps on the original list are now done (system backup/reset, landing/doctors parity, patient-facing doctor-notes edit). Migration `0011` (landing_doctors anon revoke) has been **applied and verified** live.
- **OAuth follow-ups**: Google consent screen is in "Testing" mode — access is restricted to the test users listed in the OAuth consent screen; add test emails or publish the app before real users can sign in. A full browser OAuth hand-off (click button → provider consent → callback → role-gated redirect) still needs one manual browser test per provider. Google/Yahoo sign-in only works for users whose email already matches a tenant account (created via invite/portal signup) — brand-new OAuth emails are blocked with `error=oauth_no_account` by design.
- Dark mode: currently NOT persisted across devices/sessions (localStorage is per-browser; preferences.jsonb theme survives but the FOUC init only reads localStorage). If per-device-per-user sync is wanted later, the init script should hydrate from `/api/account/preferences` on boot.
- Online payments are fully wired (initialize/webhook/callback/gateway-status + admin key UI). Before going live: the hospital must enter REAL Paystack keys in `/app/settings` and set the Paystack webhook URL to `https://<domain>/api/payments/webhook` with the matching webhook secret.
- Online payments are fully wired (initialize/webhook/callback/gateway-status + admin key UI). Before going live: the hospital must enter REAL Paystack keys in `/app/settings` and set the Paystack webhook URL to `https://<domain>/api/payments/webhook` with the matching webhook secret. Payment reference uses `SC-<tenant8>-<rand>` (unique partial index on `payments.reference`). To contract-test against Paystack sandbox use `sk_test_*` keys — `isPlaceholderKey` treats them as real.

## Commands

- Build/typecheck: `npm run build` in `frontend\` (production build also compiles all API routes).
- Dev server: `npm run dev` (port 3001 historically).
- Prod restart: kill PID listening on :3000, then `Start-Process node node_modules\next\dist\bin\next start -p 3000` (working dir `frontend`).
- DB: from `backend\supabase` run `npx supabase db push` (CLI logged in; linked prod ref).
- Verify routes: `curl.exe -s -o NUL -w "%{http_code}" http://localhost:3000/...` â€” unauth API routes should return 401, protected pages 307 â†’ /login.
- PostgREST probes (join shapes, RLS): `curl -H "apikey: $SVC_KEY" -H "Authorization: Bearer $SVC_KEY" "$SUPA_URL/rest/v1/..."` â€” read keys from `frontend/.env.local`.
- Leave `skycare--saas--hosp.txt` at repo root alone (user instruction).
